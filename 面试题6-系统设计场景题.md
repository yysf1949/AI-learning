# 企业级 Agent 面试题专题 6：系统设计 + 场景题（20 题）

> 日期：2026-06-10
> 适用：5+ 年经验 / 架构师岗 / 资深开发
> 配套：基础版 + 11 篇深挖版 + 5 篇面试题

---

## 写在前面

**系统设计题是 AI 工程师面试的"压轴"**。

**特点**：
- **没有标准答案**——但有"标准思路"
- **考查综合能力**——架构 + 工程 + 业务
- **通常 30-45 分钟**——能展开讲

**本篇 20 题，分 3 类**：
- **架构设计**（10 题）——设计一个系统
- **场景题**（6 题）——给个场景怎么办
- **故障排查**（4 题）——出问题了怎么查

---

## 第一部分：架构设计（10 题）

### ⭐⭐⭐⭐ Q1：设计一个企业级 RAG 系统

**提示词**：
> 请设计一个支持 1 万员工使用、每天 100 万次查询的企业级 RAG 系统。要求：准确率 > 90%，P95 延迟 < 2s，月成本 < ¥10 万。

**完整答案**：

#### 1. 需求分析

| 指标 | 目标 |
|---|---|
| 用户 | 1 万员工 |
| QPS | 100 万 / 86400 = **12 QPS**（平均），峰值 50 QPS |
| 准确率 | > 90% |
| 延迟 P95 | < 2s |
| 成本 | < ¥10 万/月 |

#### 2. 架构图

```
                        用户
                          ↓
                  [API Gateway] + [认证] + [限流]
                          ↓
                  [Agent Service] (Spring AI 1.0)
                          ↓
            ┌─────────┬────┴────┬─────────┐
            ↓         ↓         ↓         ↓
        [LLM 路由]  [RAG]   [Memory]  [Tools]
            ↓         ↓         ↓         ↓
        DeepSeek  Milvus   PostgreSQL   MCP Servers
        GPT-5.4   Qdrant    Redis       (HR/CRM)
            ↓
        [Observability]
        Langfuse + Prometheus
```

#### 3. LLM 选型

**主模型**：DeepSeek V4（便宜，1M 上下文）
**推理模型**：DeepSeek V4 Pro 思考模式（thinking mode，复杂问题）
**旗舰模型**：GPT-5.5（备选，1M 上下文，准确率优先）
**Embedding**：BGE-M3（中文，多语言）

**路由策略**：

| 任务类型 | 模型 | 成本 |
|---|---|---|
| 闲聊 | DeepSeek V4 Flash | ¥0.14/M |
| FAQ | DeepSeek V4 + 缓存 | ¥0.0028/M (cached) |
| 复杂问答 | DeepSeek V4 Pro | ¥0.14/M |
| 推理 | DeepSeek V4 Pro thinking | ¥1/M |
| 旗舰需求 | GPT-5.5 | ¥18/M |

#### 4. RAG 架构（6 轮调优）

```
1. 文档摄入（离线）
   - PDF / Word / Markdown / 网页
   - Recursive 切分（512 token + 50 char overlap）
   - BGE-M3 embedding
   - 写入 Milvus（HNSW 索引）
   - 标量字段（tenant_id / department / doc_type）

2. 在线查询
   - 用户问题 → BGE-M3 embedding
   - 向量检索 top 50（按 tenant_id 过滤）
   - BM25 混合检索 top 50
   - RRF 融合
   - Cross-Encoder Rerank → top 5
   - 拼 Prompt → LLM 生成
   - 加引用编号
```

#### 5. 性能优化

- **Caffeine L1 缓存**——相同 query 5 分钟
- **Redis L2 缓存**——相同 query 1 小时
- **Prompt Caching**——DeepSeek 50x 折扣
- **流式响应**——TTFT < 500ms
- **连接池**——HTTP 连接复用

#### 6. 成本估算

**单次查询成本**：
- Input: 1K token × ¥0.14/M = ¥0.00014
- Output: 0.5K token × ¥0.28/M = ¥0.00014
- 总：¥0.00028

**月度**：
- 100万次 × ¥0.00028 = **¥2800/月** ✅（远低于 ¥10 万）

**加分项**：
- 提到 **Ragas / DeepEval 评估**
- 提到 **A/B 测试 Prompt 优化**
- 提到 **多租户隔离**

---

### ⭐⭐⭐⭐ Q2：设计一个智能客服 Agent

**提示词**：
> 设计一个电商客服 Agent，能回答订单查询、退换货、政策咨询。要求：70% 问题自动解决，30% 转人工。

**完整答案**：

#### 1. 意图分类（核心）

```java
public enum Intent {
    ORDER_QUERY,        // 订单查询
    RETURN_REFUND,      // 退换货
    POLICY_QUESTION,    // 政策咨询
    COMPLAINT,          // 投诉
    CHITCHAT,           // 闲聊
    ESCALATE_TO_HUMAN   // 转人工
}

// 用 LLM 分类
String intent = llm.prompt()
    .system("""
        你是意图分类器。
        从以下类别中选一个：ORDER_QUERY / RETURN_REFUND / POLICY_QUESTION / COMPLAINT / CHITCHAT / ESCALATE_TO_HUMAN
        """)
    .user(userMessage)
    .call()
    .content();
```

#### 2. 不同意图的处理

```
ORDER_QUERY
    ↓
   Tool: query_order(order_id)
    ↓
   回答用户
   
RETURN_REFUND
    ↓
   Tool: check_return_eligibility(order_id, reason)
    ↓
   如果可退 → 创建退款单
   如果不可退 → 解释原因
   
POLICY_QUESTION
    ↓
   RAG 检索公司政策
    ↓
   LLM 生成答案 + 引用
   
COMPLAINT
    ↓
   情感分析（高负面 → 升级）
    ↓
   如果紧急 → ESCALATE_TO_HUMAN
   否则 → 记录工单 + 回复
   
CHITCHAT
    ↓
   小模型闲聊（DeepSeek V4 Flash）
   
ESCALATE_TO_HUMAN
    ↓
   1. 创建人工工单（含上下文）
   2. 通知人工坐席
   3. 等待接入
```

#### 3. 关键设计

**多轮对话**：用 Redis 存对话历史
**人工介入**：检测到 ESCALATE_TO_HUMAN → 转人工
**高危操作**：退款 > ¥1000 → 人工审批
**情感分析**：高负面情绪 → 升级

#### 4. 完整架构

```
用户 → API Gateway → Agent
                          ↓
                    [意图分类]
                          ↓
            ┌────┬────┬────┬────┬────┐
            ↓    ↓    ↓    ↓    ↓    ↓
         订单  退换  政策  投诉 闲聊  人工
            ↓    ↓    ↓    ↓    ↓    ↓
         Tool  Tool  RAG  Tool  LLM  转接
            ↓    ↓    ↓    ↓    ↓
         缓存  审批  知识库 工单  -  坐席
```

**加分项**：
- 提到 **情感分析**触发升级
- 提到 **多轮对话状态管理**
- 提到 **人工坐席接管 + 上下文同步**

---

### ⭐⭐⭐⭐ Q3：设计一个代码助手 Agent

**提示词**：
> 设计一个企业内部代码助手 Agent，能查代码库、生成代码、解释代码、提 PR。要求：80% 任务 1 分钟内完成。

**完整答案**：

#### 1. 能力清单

| 能力 | 工具 |
|---|---|
| 查代码 | `search_code(query)` — 用 Milvus + BM25 |
| 读文件 | `read_file(path, offset, limit)` |
| 写文件 | `write_file(path, content)` |
| 跑测试 | `run_tests(path)` |
| Git 操作 | `git_commit / git_push / create_pr` |
| 代码解释 | LLM 解释 |
| 生成代码 | LLM + RAG 检索参考 |

#### 2. 关键工具实现

**search_code**（基于 RAG）：
```java
@McpTool(description = "搜索代码库")
public List<CodeChunk> searchCode(
    @McpParam(description = "查询字符串") String query
) {
    // 1. 向量检索
    List<CodeChunk> vectorResults = milvus.similaritySearch(query, 30);
    // 2. BM25 检索
    List<CodeChunk> bm25Results = elasticsearch.search(query, 30);
    // 3. RRF 融合
    return rrf(vectorResults, bm25Results, 20);
}
```

**create_pr**（高危操作，审批）：
```java
@McpTool(description = "创建 PR")
public String createPr(
    @McpParam(description = "分支名") String branch,
    @McpParam(description = "PR 标题") String title,
    @McpParam(description = "PR 内容") String body
) {
    // 1. CI 检查
    boolean ciPass = runCI(branch);
    if (!ciPass) {
        return "CI 失败，请先修复";
    }

    // 2. 至少 1 个 reviewer 审批（Code Owners）
    String approvalId = approvalService.requestApproval(
        "code_review",
        Map.of("branch", branch, "title", title, "body", body)
    );
    return "已创建 PR，等待 Reviewer 审批：" + approvalId;
}
```

#### 3. Agent 流程

```
用户："帮我修一下 UserService 的 N+1 查询"
    ↓
1. 意图理解（修 bug / 加功能 / 解释）
    ↓
2. 找文件（search_code "UserService N+1"）
    ↓
3. 读文件（read_file UserService.java）
    ↓
4. 分析问题（LLM 推理）
    ↓
5. 写修复（write_file UserService.java）
    ↓
6. 跑测试（run_tests UserServiceTest.java）
    ↓
7. 提交 PR（create_pr "fix-n1"）
    ↓
8. 报告结果
```

#### 4. 沙箱

- 读文件：只读权限
- 写文件：限定工作目录
- 跑测试：Docker 隔离
- 提交 PR：必须审批

**加分项**：
- 提到 **Code Embedding 专用模型**（CodeBERT / StarCoder Embedding）
- 提到 **AST 解析**（更精确的代码切分）
- 提到 **Git 沙箱**（限定分支）

---

### ⭐⭐⭐⭐ Q4：设计一个多 Agent 协作系统（销售 + 客服 + 财务）

**提示词**：
> 设计一个支持销售、客服、财务 3 个 Agent 协作的系统。销售 Agent 需要在客户咨询时自动调客服 Agent 查历史，财务 Agent 需要在销售合同时自动审预算。

**完整答案**：

#### 1. Agent 角色

| Agent | 职责 | 暴露能力 |
|---|---|---|
| **销售 Agent** | 客户管理、报价 | `query_customer`, `create_quote` |
| **客服 Agent** | 工单、历史 | `query_tickets`, `get_history` |
| **财务 Agent** | 预算、发票 | `check_budget`, `create_invoice` |

#### 2. A2A 协作

```
销售 Agent (Spring AI)
   ├── [A2A] 客服 Agent
   │   → query_tickets(customer_id) → 历史工单
   │
   └── [A2A] 财务 Agent
       → check_budget(amount) → 预算是否够
```

#### 3. 关键实现

**Agent Card 注册**：

```yaml
# sales-agent
name: 销售助手
url: https://sales-agent.company.com
skills:
  - query_customer
  - create_quote
auth: oauth2
scopes: [read:customer, write:quote]

# finance-agent
name: 财务助手
url: https://finance-agent.company.com
skills:
  - check_budget
  - create_invoice
auth: oauth2
scopes: [read:budget, write:invoice]
```

**自动发现 + 调用**：

```java
@Service
public class SalesAgent {

    public String handleQuery(String userQuery) {
        // 1. 查客户信息
        Customer customer = a2aClient.callSkill(
            "https://sales-agent", "query_customer", "C-001"
        );

        // 2. 查历史工单（调客服 Agent）
        List<Ticket> tickets = a2aClient.callSkill(
            "https://support-agent", "query_tickets", customer.getId()
        );

        // 3. 拼答案
        return llm.prompt()
            .user("客户 %s 问：%s\n历史工单：%s\n回答：".formatted(
                customer.getName(), userQuery, tickets))
            .call()
            .content();
    }
}
```

#### 4. 数据流

```
客户咨询
   ↓
销售 Agent 接收
   ↓
[A2A] 调客服 Agent → 查工单
   ↓
[A2A] 调财务 Agent → 查预算
   ↓
[LLM] 综合所有信息 → 回复
```

#### 5. 治理

- **注册中心**——Nacos / Consul
- **健康检查**——剔除不健康 Agent
- **限流**——每 Agent 每分钟 1000 次
- **审计**——记录所有跨 Agent 调用
- **熔断**——失败 3 次自动熔断

**加分项**：
- 提到 **任务超时**——5s 没回自动 fallback
- 提到 **结果缓存**——相同 query 1 小时
- 提到 **调用链追踪**（OTel）

---

### ⭐⭐⭐⭐ Q5：设计一个金融 RAG（合规要求高）

**提示词**：
> 设计一个金融行业的 RAG 系统，要求：审计合规、数据隔离、监管报送支持。500 份合规文档，100 万次/月查询。

**完整答案**：

#### 1. 金融合规要求

| 要求 | 实现 |
|---|---|
| **数据本地化** | 自建 K8s，不上公有云 |
| **审计日志** | 所有访问记录（不可删除）|
| **数据隔离** | 多租户 / 部门级 |
| **监管报送** | 标准化输出（JSON）|
| **SLA** | 99.9% 可用 |
| **加密** | 落盘 + 传输 |

#### 2. 特殊设计

**Prompt 防泄露**：
```java
String safeSystemPrompt = """
    你是金融合规助手。
    严格基于【权威文档】回答。
    任何与文档不符的信息，回复"未在权威文档中找到"。
    不得编造法规、判例、利率。
    所有答案必须标注引用编号 [1] [2]。
""";
```

**审计日志**（不可删除）：
```java
@Aspect
public class AuditAspect {
    @Around("execution(* com.bank.rag..*(..))")
    public Object audit(ProceedingJoinPoint pjp) throws Throwable {
        // 1. 记录访问前
        String traceId = UUID.randomUUID().toString();
        auditLog.record("rag_access", UserContext.get(), traceId,
            jp.getArgs(), Instant.now());

        // 2. 执行
        Object result = pjp.proceed();

        // 3. 记录访问后（含结果）
        auditLog.record("rag_response", traceId, result, Instant.now());

        return result;
    }
}
```

**存储 WORM**（Write Once Read Many）：
```java
// 审计日志用 S3 Object Lock（不可删）
S3Client s3 = S3Client.builder().build();
s3.putObject(PutObjectRequest.builder()
    .bucket("bank-audit")
    .key("audit/" + traceId)
    .objectLockMode(ObjectLockMode.COMPLIANCE)  // 合规模式：不可删
    .objectLockRetainUntilDate(Instant.now().plus(7, ChronoUnit.YEARS))
    .build(), RequestBody.fromString(jsonAudit));
```

**数据隔离**（行级 + Collection 级）：

```java
// Milvus 多 Collection（按部门）
List<String> departments = List.of("retail", "corporate", "risk");
for (String dept : departments) {
    milvus.createCollection("rag_" + dept);
}

// 检索时按部门过滤
SearchRequest req = SearchRequest.builder()
    .query(query)
    .filterExpression("department == '" + userDept + "'")
    .build();
```

**监管报送**（JSON 标准化）：

```java
@Data
public class RegulatorReport {
    private String queryId;
    private String userId;
    private String question;
    private String answer;
    private List<String> citations;  // 引用编号
    private double confidence;
    private String modelVersion;
    private Instant timestamp;
}
```

#### 3. 高可用 + DR

| 故障 | RTO | 方案 |
|---|---|---|
| 主 LLM 厂商挂 | < 1min | Failover 到备用厂商 |
| 向量库挂 | < 5min | 切到只读备份 |
| Region 挂 | < 30min | 切 Region |

**加分项**：
- 提到 **金[^1] 监管报送**——文档结构化
- 提到 **数据脱敏**（PII 自动 mask）
- 提到 **同义改写审查**——人工抽检 1% 答案

---

### ⭐⭐⭐⭐ Q6：设计一个高 QPS LLM 网关

**提示词**：
> 设计一个支持 1 万 QPS 的 LLM 网关，要求：多模型路由、限流、缓存、计费、审计。

**完整答案**：

#### 1. 网关功能

```
请求进入
   ↓
[认证] → [限流] → [路由] → [缓存] → [LLM 调用] → [计费] → [审计] → 返回
```

#### 2. 详细设计

**认证**：
```java
public class AuthFilter implements Filter {
    public void doFilter(...) {
        String token = extractToken(req);
        Claims claims = jwtValidator.parse(token);
        TenantContext.set(claims.get("tenant_id"));
    }
}
```

**限流**（Redis 滑动窗口）：
```lua
-- 100 QPS / 用户
local key = KEYS[1]
local limit = 100
local current = redis.call('INCR', key)
if current == 1 then redis.call('EXPIRE', key, 1) end
if current > limit then return 0 end
return 1
```

**路由**（智能模型选择）：
```java
public String route(Request req) {
    if (req.isSimple()) return "deepseek-v4-flash";
    if (req.isReasoning()) return "deepseek-v4-pro";  // V4 Pro thinking mode
    if (req.isPremium()) return "gpt-5.5";
    return "deepseek-v4-flash";  // default
}
```

**缓存**（多级）：
```java
// L1: Caffeine（1 分钟，命中率 20%）
// L2: Redis（1 小时，命中率 30%）
// L3: 厂商 Prompt Cache（命中率高）
```

**计费**：
```java
public void bill(Usage usage) {
    double cost = usage.inputTokens * 0.14 / 1_000_000
                + usage.outputTokens * 0.28 / 1_000_000;
    billingClient.charge(TenantContext.get(), cost);
}
```

**审计**：
```java
public void audit(Request req, Response resp) {
    AuditEvent event = new AuditEvent(
        UUID.randomUUID().toString(),
        req.userId, req.tenantId, req.model,
        req.prompt, resp.content, resp.tokens, resp.cost
    );
    kafka.send("llm-audit", event);
}
```

#### 3. 性能要求

- **延迟**：P99 < 50ms（网关本身，不含 LLM）
- **吞吐**：1 万 QPS
- **可用性**：99.99%

**架构**：
- 4 个网关 Pod（负载均衡）
- Redis Cluster（限流 + 缓存）
- Kafka（审计 + 计费异步）
- OTel（Trace 串联）

**加分项**：
- 提到 **零信任安全模型**
- 提到 **多租户配额管理**
- 提到 **A/B 测试支持**

---

### ⭐⭐⭐⭐ Q7：设计一个 Agent 沙箱平台

**提示词**：
> 设计一个能运行第三方 Agent 的沙箱平台，要求：完全隔离、可信执行、资源限制、审计。

**完整答案**：

#### 1. 沙箱层级

```
L1 进程隔离（Docker）
L2 容器隔离（gVisor / Firecracker）
L3 操作系统隔离（K8s Namespace）
L4 网络隔离（VPC Peering）
L5 资源限制（CPU / 内存 / 磁盘 IO）
```

#### 2. 完整架构

```
              第三方 Agent 代码
                     ↓
        [代码审查]  ← 安全扫描（Checkov / Trivy）
                     ↓
        [签名验证]  ← SBOM + 数字签名
                     ↓
       [Docker 镜像构建]  ← 多阶段
                     ↓
       [gVisor 沙箱启动]  ← 隔离执行
                     ↓
       [K8s 部署]  ← 资源限制
                     ↓
       [NetworkPolicy]  ← 网络隔离
                     ↓
       [审计日志]  ← 所有操作记录
```

#### 3. 关键配置

**gVisor 沙箱**：

```yaml
apiVersion: node.k8s.io/v1
kind: RuntimeClass
metadata:
  name: gvisor-agent
handler: runsc  # gVisor
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: third-party-agent
spec:
  template:
    spec:
      runtimeClassName: gvisor-agent
      containers:
      - name: agent
        image: third-party/agent:v1.0.0
        resources:
          limits:
            cpu: "1"
            memory: "1Gi"
        securityContext:
          readOnlyRootFilesystem: true
          runAsNonRoot: true
          capabilities:
            drop: [ALL]
```

**网络隔离**：

```yaml
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: agent-isolation
spec:
  podSelector:
    matchLabels:
      role: third-party-agent
  policyTypes:
  - Ingress
  - Egress
  egress:
  - to:
    - namespaceSelector:
        matchLabels:
          name: llm-services  # 只允许访问 LLM
```

**资源限制**：

```java
// 限制每个 Agent 的 API 调用
public class AgentRateLimiter {
    public void check(String agentId, String tool) {
        String key = "agent:" + agentId + ":" + tool;
        Long count = redis.increment(key);
        if (count > 1000) {  // 每小时 1000 次
            throw new RateLimitExceededException();
        }
    }
}
```

#### 4. 审计

```java
@Aspect
public class SandboxAuditAspect {
    @Around("execution(* third.party..*(..))")
    public Object audit(ProceedingJoinPoint pjp) throws Throwable {
        long start = System.currentTimeMillis();
        try {
            Object result = pjp.proceed();
            sandboxLog.record("call", pjp.getArgs(), result,
                System.currentTimeMillis() - start);
            return result;
        } catch (Exception e) {
            sandboxLog.record("error", pjp.getArgs(), e.getMessage(), start);
            throw e;
        }
    }
}
```

**加分项**：
- 提到 **WASM 沙箱**（更轻量）
- 提到 **机密计算**（Intel SGX）
- 提到 **第三方 Agent 风险评估**

---

### ⭐⭐⭐⭐ Q8：设计一个成本可控的 LLM 系统

**提示词**：
> 你的 LLM 系统每月成本 ¥50 万，老板要求降到 ¥10 万。怎么设计？

**完整答案**：

#### 1. 现状分析

先**算清账**：

| 维度 | 数据 |
|---|---|
| 调用次数 | 100 万/月 |
| 平均 input | 5K token |
| 平均 output | 2K token |
| 主要模型 | GPT-5.5（¥18/M input，1M 上下文）|
| **成本** | 100万 × 5K × 18/1M + 100万 × 2K × 60/1M = **¥21 万**（LLM）|

#### 2. 7 个优化方向

| 优化 | 节省 | 难度 |
|---|---|---|
| **1. 智能路由**（小模型 + 大模型）| 50% | 中 |
| **2. Prompt Caching** | 30% | 低 |
| **3. 输入压缩** | 20% | 中 |
| **4. 流式 + 取消** | 10% | 中 |
| **5. 批处理** | 5% | 低 |
| **6. 缓存层**（相同 query 复用）| 40% | 中 |
| **7. 任务降级**（不重要任务用小模型）| 30% | 中 |

**总节省**：70-80%

#### 3. 详细方案

**智能路由**（节省 50%）：

```java
public class ModelRouter {
    public String route(Request req) {
        if (req.isSimple()) return "deepseek-v4-flash";  // 便宜
        if (req.isComplex()) return "gpt-5.5";            // 旗舰
        if (req.isReasoning()) return "deepseek-v4-pro";  // V4 Pro thinking mode
        return "deepseek-v4-flash";
    }
}
```

**Prompt Caching**（节省 30%）：

```java
// 静态内容放最前
String staticPrefix = "你是 HR 助手，公司制度如下：...";  // 5K token
String userQuery = "...";  // 变化
// DeepSeek cache read 0.0028/M，50x 便宜
```

**输入压缩**（节省 20%）：

```java
public String compress(String text) {
    // 1. 移除空白
    text = text.replaceAll("\\s+", " ");
    // 2. 提取关键句
    text = extractKeySentences(text, 0.5);  // 保留 50% 句子
    return text;
}
```

**缓存层**（节省 40%）：

```java
@Cacheable(value = "llm_response", key = "#query", unless = "#result == null")
public String chat(String query) {
    return chatClient.prompt().user(query).call().content();
}
```

#### 4. 优化后账单

| 优化前 | 优化后 |
|---|---|
| ¥21 万（LLM）| ¥6 万（LLM）|
| ¥2 万（Embedding）| ¥0.6 万（缓存）|
| ¥1 万（向量库）| ¥1 万（不变）|
| ¥0.5 万（监控）| ¥0.5 万（不变）|
| **¥24.5 万** | **¥8.1 万** |

**优化后**：

- 智能路由：-50% = ¥3 万
- Prompt Cache：-30% = ¥0.9 万
- 输入压缩：-20% = ¥0.6 万
- 缓存层：-40% = ¥1.2 万
- **总 LLM 成本**：¥3 + ¥0.9 + ¥0.6 + ¥1.2 = **¥5.7 万** ✅

**比 ¥10 万预算还低 ¥4.3 万**。

**加分项**：
- 提到 **Token 计数器先行**（优化前先量化）
- 提到 **分租户计费**（让业务方有意识）
- 提到 **告警**（超预算告警）

---

### ⭐⭐⭐⭐ Q9：设计一个可观测的 Agent 系统

**提示词**：
> 设计一个完整的 Agent 可观测性系统，能监控性能、成本、质量、安全。

**完整答案**：

#### 1. 4 大支柱

```
                 Agent 系统
                       ↓
        ┌──────────┬───┴───┬──────────┐
        ↓          ↓       ↓          ↓
     [Metrics]  [Traces] [Logs]   [Evals]
        ↓          ↓       ↓          ↓
    Prometheus  Jaeger  Loki    Ragas
        ↓          ↓       ↓          ↓
        └──────────┴───┬───┴──────────┘
                       ↓
                   Grafana 看板
                       ↓
                  Alertmanager 告警
```

#### 2. Metrics 指标

**4 层指标**：

| 层 | 指标 | 工具 |
|---|---|---|
| **Infra** | CPU / 内存 / 磁盘 / 网络 | node_exporter |
| **App** | QPS / 延迟 / 错误率 | Micrometer |
| **LLM** | TTFT / TPS / Token / 成本 | 自定义 |
| **Business** | 采纳率 / 转化率 / 用户反馈 | 自定义 |

#### 3. Traces 追踪

```java
// Spring AI 自动埋点
@Bean
public ObservationRegistry observationRegistry() {
    return ObservationRegistry.create();
}

chatClient.prompt()
    .user(query)
    .observationRegistry(observationRegistry)  // 自动埋点
    .call()
    .content();

// Trace Span：
//   HTTP POST /chat (1.8s)
//   ├── LLM Call #1 (200ms)
//   ├── Tool Call: get_weather (300ms)
//   ├── Tool Call: query_customer (200ms)
//   └── LLM Call #2 (1.1s)
```

#### 4. Logs 结构化

```java
log.info("llm_call",
    StructuredArguments.kv("model", "deepseek-v4-flash"),
    StructuredArguments.kv("input_tokens", 1500),
    StructuredArguments.kv("output_tokens", 800),
    StructuredArguments.kv("cost_usd", 0.00042),
    StructuredArguments.kv("user_id", userId),
    StructuredArguments.kv("trace_id", traceId)
);
```

#### 5. Evaluations 质量

```java
// 自动化评估（每次发布跑）
@Test
void testRagQuality() {
    for (TestCase test : goldenTestSet) {
        AnswerRelevancyMetric metric = new AnswerRelevancyMetric();
        double score = metric.measure(test);
        assertTrue(score > 0.8);
    }
}
```

#### 6. 告警分级

```yaml
- alert: LLMHighErrorRate
  expr: rate(llm_errors[5m]) / rate(llm_total[5m]) > 0.1
  severity: P1

- alert: LLMHighLatency
  expr: histogram_quantile(0.99, llm_duration_seconds_bucket) > 5
  severity: P1

- alert: LLMHighCost
  expr: increase(llm_cost_usd[1h]) > 1000
  severity: P2
```

**加分项**：
- 提到 **Langfuse**（LLM 专用）
- 提到 **Ragas / DeepEval**
- 提到 **A/B 测试**（Prompt 优化）

---

### ⭐⭐⭐⭐⭐ Q10：设计一个完全自动化的 Agent CI/CD 平台

**提示词**：
> 设计一个 Agent 的 CI/CD 平台，要求：自动测试、自动部署、自动回滚、灰度发布。

**完整答案**：

#### 1. 完整流程

```
代码 push / Prompt 改
   ↓
[CI: 单元测试 + 集成测试 + 评估]
   ↓
[构建: Docker 镜像]
   ↓
[部署: Staging 环境]
   ↓
[集成测试: 端到端]
   ↓
[Canary: 5% 流量]
   ↓
[监控: 5min 观察]
   ↓
[扩量: 25% → 50% → 100%]
   ↓
[全量发布]
```

#### 2. 关键组件

**CI（GitHub Actions）**：

```yaml
name: Agent CI
on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: 单元测试
      run: mvn test

    - name: 集成测试（需 API Key）
      env:
        DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
      run: mvn verify

    - name: 评估（DeepEval）
      env:
        OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
      run: python -m deepeval test run

    - name: 红队测试（Promptfoo）
      run: npx promptfoo redteam run
```

**CD（Argo Rollouts）**：

```yaml
apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: agent-service
spec:
  replicas: 10
  strategy:
    canary:
      steps:
      - setWeight: 5     # 5%
      - pause: {duration: 5m}
      - setWeight: 25    # 25%
      - pause: {duration: 5m}
      - setWeight: 50    # 50%
      - pause: {duration: 5m}
      - setWeight: 100
  analysis:
    templates:
    - templateName: agent-metrics
```

**自动回滚**：

```yaml
- templateName: agent-metrics
  args:
  - name: error-rate
    value: |
      rate(agent_errors[2m]) / rate(agent_total[2m])
  - name: latency-p99
    value: |
      histogram_quantile(0.99, agent_duration_seconds_bucket)
  failureCondition: result[0] > 0.05 || result[1] > 5
  consecutiveErrorLimit: 2  # 连续 2 次失败 → 自动回滚
```

#### 3. Prompt 版本管理

```java
@Service
public class PromptRegistry {
    // 存到 Nacos
    @NacosValue(value = "${prompt.system}", autoRefreshed = true)
    private String systemPrompt;

    // 灰度发布
    public String getPrompt(String userId) {
        if (isInCanary(userId)) {
            return canaryPrompt;  // 5% 用户
        }
        return stablePrompt;
    }
}
```

**加分项**：
- 提到 **A/B 测试框架**
- 提到 **Feature Flag**（LaunchDarkly / 自建）
- 提到 **ChatOps**（用 Slack 控制发布）

---

## 第二部分：场景题（6 题）

### ⭐⭐⭐⭐ Q11：客服 Agent 把用户隐私信息泄露给另一个用户，怎么排查？

**参考答案**：

**排查步骤**：

1. **看审计日志**——找到泄露的 Trace ID
2. **复现 Trace**——用 Langfuse / Braintrust 看到底传了什么
3. **定位根因**：
   - 缓存串了？（Redis 没按 tenant_id 分）
   - 数据库查询错了？（少了 tenant_id 过滤）
   - Prompt 串了？（system prompt 没带 tenant）
   - RAG 检索错了？（vector search 漏了过滤）
4. **修复 + 防同类**

**常见 5 大原因**：

| # | 原因 | 修复 |
|---|---|---|
| 1 | Redis cache key 没带 tenant | 加 tenant_id 到 key |
| 2 | DB 查询漏了 tenant_id 过滤 | 用 MyBatis-Plus @TenantId |
| 3 | RAG 检索没过滤 | 加 filterExpression |
| 4 | Prompt 注入（用户问出其他租户）| 加防御 |
| 5 | 会话串了（WebSocket 错连）| 严格绑定 userId |

**加分项**：
- 提到 **租户隔离测试**——自动化
- 提到 **"红队"测试**——故意找漏洞

---

### ⭐⭐⭐ Q12：RAG 系统准确率从 90% 掉到 70%，怎么办？

**参考答案**：

**排查步骤**：

1. **看监控**——哪个指标变了？
   - 召回率变 → Embedding / 索引问题
   - 答案质量变 → LLM 问题
   - 延迟变 → 性能问题

2. **黄金测试集**——跑 100 个标准 QA，看哪类问题失败多

3. **分阶段排查**：
   - **文档摄入**——新文档格式变了？
   - **切分**——新文档被错切？
   - **Embedding**——换了模型？
   - **检索**——新加的文档没索引？
   - **Rerank**——Rerank 模型挂了？
   - **Prompt**——Prompt 被改坏了？

**常见 5 大原因**：

| 原因 | 修复 |
|---|---|
| 新文档没摄入 | 触发离线 pipeline |
| Embedding 模型换了 | 回滚 |
| Prompt 改了 | 用 A/B 测 |
| 索引刷爆 | 手动 rebuild |
| 用户问题变了 | 加新训练数据 |

**加分项**：
- 提到 **监控告警**——准确率掉 10% 自动告警
- 提到 **A/B 回滚**——1 分钟回滚

---

### ⭐⭐⭐ Q13：Agent 突然变慢 10 倍，怎么办？

**参考答案**：

**排查步骤**：

1. **看大盘**——P99 涨到多少？
2. **看 Trace**——哪个 Span 慢？
3. **分模块排查**：

| 模块 | 检查 | 工具 |
|---|---|---|
| **API 网关** | 网关 CPU / 网络 | Prometheus |
| **Agent 服务** | JVM / 线程池 / DB 连接 | Arthas |
| **LLM API** | 厂商状态页 | DeepSeek Status |
| **向量库** | segment 数 / 索引 | Milvus Metrics |
| **数据库** | 慢查询 | PG slow log |
| **缓存** | 命中率 | Redis INFO |

**常见原因**：

| 原因 | 修复 |
|---|---|
| 向量库索引刷爆 | 手动 trigger 索引 |
| DB 慢查询 | 优化 SQL / 加索引 |
| 缓存命中率掉 | 检查 Redis |
| LLM 厂商慢 | 切备用厂商 |
| 流量涨 | 加机器 / 限流 |
| 死循环 | 加最大步数限制 |

**加分项**：
- 提到 **故障 Runbook**——常见问题处理手册
- 提到 **混沌工程**（主动测）

---

### ⭐⭐⭐⭐ Q14：客户反馈 Agent 答非所问，怎么分析？

**参考答案**：

**5 大可能**：

1. **RAG 检索不到相关内容**（最常见 50%）
2. **RAG 检索到但不相关**（30%）
3. **LLM 答错了**（10%）
4. **用户问题太模糊**（5%）
5. **Prompt 问题**（5%）

**分析步骤**：

1. **抽样 100 个用户反馈**——人工分类
2. **跑 Ragas 4 指标**：
   - Faithfulness（忠实度）
   - Answer Relevancy（答案相关）
   - Context Precision（检索精准）
   - Context Recall（检索召回）
3. **定位**：
   - **Context Recall 低** → 加 BM25 / Rerank
   - **Context Precision 低** → 调 Rerank / Prompt
   - **Faithfulness 低** → Prompt 加约束
   - **Answer Relevancy 低** → 改 Prompt

**加分项**：
- 提到 **用户反馈环路**——点赞/点踩 → 训练数据
- 提到 **A/B 测试**——逐步优化

---

### ⭐⭐⭐ Q15：Agent 调用了不该调的工具，怎么防？

**参考答案**：

**4 层防御**：

1. **工具白名单**——OPA 鉴权
2. **参数校验**——SQL 注入 / 路径穿越
3. **沙箱**——Docker / gVisor
4. **高危操作审批**——人机协同

**实战**：

```java
// OPA 鉴权
public class ToolAuthAdvisor {
    public ChatClientRequest before(ChatClientRequest req) {
        String toolName = req.getToolCall().getName();
        if (!opaClient.check(UserContext.get().userId(), toolName)) {
            throw new ForbiddenException("无权限调 " + toolName);
        }
        return req;
    }
}

// 参数校验
@McpTool(description = "执行 SQL（只读）")
public List<Map<String, Object>> query(String sql) {
    if (!isValidSelect(sql)) throw new IllegalArgumentException();
    if (extractTable(sql) not in ALLOWED_TABLES) throw new IllegalArgumentException();
}

// 沙箱
@McpTool(description = "执行 Python（沙箱）")
public String executePython(String code) {
    return dockerSandbox.run(code);  // 无网络 + 256MB
}

// 高危操作审批
@McpTool(description = "转账（需审批）")
public String transfer(BigDecimal amount) {
    if (amount > 10000) {
        return approvalService.request(...);
    }
    return transferService.execute(...);
}
```

**加分项**：
- 提到 **红队测试**——故意找漏洞
- 提到 **审计 + 告警**——异常操作告警

---

### ⭐⭐⭐ Q16：LLM 厂商挂了，系统怎么不挂？

**参考答案**：

**3 层 Failover**：

1. **多 Key 轮询**——同厂商多 Key
2. **多厂商 Failover**——DeepSeek → 阿里 → OpenAI
3. **本地模型 Fallback**——Ollama + 小模型

**Resilience4j 熔断**：

```java
@CircuitBreaker(name = "deepseek", fallbackMethod = "fallback")
public String callDeepSeek(String query) {
    return deepseek.call(query);
}

private String fallback(String query, Throwable t) {
    // 1. 试阿里
    try {
        return qwen.call(query);
    } catch (Exception e) {
        // 2. 试本地小模型
        return ollama.call(query);
    }
}
```

**配置**：

```yaml
resilience4j:
  circuitbreaker:
    instances:
      deepseek:
        slidingWindowSize: 100
        failureRateThreshold: 50
        waitDurationInOpenState: 30s
```

**加分项**：
- 提到 **降级 3 级**——错误 / 简化 / 排队
- 提到 **流量控制**——超阈值排队
- 提到 **Lighthouse**（Blackbox 监控）

---

## 第三部分：故障排查（4 题）

### ⭐⭐⭐ Q17：LLM 偶尔返回 200 但内容是空字符串

**参考答案**：

**可能原因**：

1. **流式响应处理 bug**——SSE 解析丢包
2. **LLM 触发安全过滤**——返回空 + 状态码 200
3. **Prompt 触发 LLM 拒绝**——输出 `<空>`
4. **网络问题**——TCP 截断
5. **Token 超限**——max_tokens = 0

**排查**：

```java
// 1. 看 Langfuse Trace → LLM 实际返回了什么
// 2. 看监控 → 错误率涨了吗
// 3. 看日志 → finish_reason 是什么

ChatResponse resp = chatClient.prompt().user(query).call().chatResponse();
String finishReason = resp.getMetadata().getFinishReason();
if ("content_filter".equals(finishReason)) {
    // 触发了安全过滤
}
if ("length".equals(finishReason)) {
    // 达到了 max_tokens
}
```

**修复**：
- 重试（带 backoff）
- 切换备用模型
- 告警

**加分项**：
- 提到 **finish_reason 必埋点**
- 提到 **空响应的 fallback**（用本地模型补一句）

---

### ⭐⭐⭐ Q18：向量库 P99 突然涨 10 倍

**参考答案**：

**常见原因**：

| # | 原因 | 排查 |
|---|---|---|
| 1 | HNSW 索引被刷爆 | 看 segment 数（> 1000 要合并）|
| 2 | 新数据持续写入 | 看 ingestion 速率 |
| 3 | 慢查询 | 看慢查询日志 |
| 4 | 磁盘 IO 满了 | 看 iostat |
| 5 | 内存不足 | 看 JVM / OS 内存 |

**排查步骤**：

```bash
# 1. 看 Milvus metrics
curl http://milvus:9091/metrics | grep query_latency

# 2. 看 segment 数
curl http://milvus:9091/metrics | grep segment

# 3. 看慢查询
grep "search_latency > 1s" milvus.log

# 4. 看磁盘
df -h /milvus-data
iostat -x 1
```

**修复**：

```java
// 1. 手动 trigger 索引合并
milvus.compact(collection);

// 2. 限制 segment 上限
@Scheduled(cron = "0 * * * * ?")
public void compact() {
    if (milvus.getSegmentCount() > 1000) {
        milvus.compact();
    }
}

// 3. 切到只读备份
failover.toReadReplica();
```

**加分项**：
- 提到 **告警**——segment > 800 告警
- 提到 **熔断**——慢查询比例 > 30% 熔断

---

### ⭐⭐⭐ Q19：Agent 死循环一直调同一个工具

**参考答案**：

**检测**：

```java
@Aspect
public class LoopDetectionAdvisor {
    @Before
    public void check(ChatClientRequest req) {
        // 1. 统计 Tool 调用次数
        int toolCount = countToolCalls(req);
        if (toolCount > 20) {
            throw new RuntimeException("工具调用超过 20 次");
        }

        // 2. 检测重复
        if (isRepeatingSameCall(req, 3)) {
            throw new RuntimeException("检测到重复调用");
        }
    }
}
```

**根因**：
- LLM 反复"想不通"
- 工具返回错误，LLM 不知道怎么办
- Prompt 模糊

**修复**：

1. **限制最大步数**
2. **工具返回明确错误**（让 LLM 能学）
3. **Prompt 改清晰**

**加分项**：
- 提到 **Fallback**——超步数返回"我没想明白"
- 提到 **循环检测算法**（连续 3 次相同工具 + 参数）

---

### ⭐⭐⭐⭐ Q20：RAG 答案质量突然下降，但所有指标都正常

**参考答案**：

**可能原因（指标看不到的）**：

1. **新文档污染**——有人上传了错误数据
2. **Prompt 被改**（微调 / 误操作）
3. **Embedding 模型升级**（变了语义）
4. **用户问题分布变了**（新业务）
5. **业务方主观感受变化**

**排查**：

```java
// 1. 抽样 50 个用户反馈
// 2. 跑 golden test set
// 3. 对比上周/上月的 Ragas 分数
// 4. 查文档摄入日志
// 5. 查 Prompt 版本
```

**修复**：

- 文档问题 → 删除污染文档
- Prompt 问题 → 回滚 Prompt 版本
- 业务变化 → 重新训练 / 加新数据
- 用户感受 → 收集更多反馈

**加分项**：
- 提到 **Prompt 版本管理**（Git）
- 提到 **文档审核**——人工 review
- 提到 **A/B 测试**——隔离问题

---

## 写在最后

**这 20 题系统设计 / 场景 / 故障题，是面试的"压轴"。**

**核心准备建议**：

1. **架构题准备 5 个常见场景**——RAG / 客服 Agent / 智能体 / 网关 / 监控
2. **场景题准备 3 步走**——先看监控 → 再复现 → 最后定位
3. **故障题准备常见 Runbook**——HNSW 索引 / 死循环 / Failover

**配合前 5 篇面试题**——你已经能应对 95% 的企业级 Agent 面试了。

---

