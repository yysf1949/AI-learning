# 企业级 Agent 面试题专题 4：企业级工程（性能/成本/可观测/安全）（25 题）

> 日期：2026-06-10
> 适用：3-5 年 LLM 应用经验 / 架构师岗
> 配套：深挖版 2/3/5/7（性能/可观测/部署/安全）

---

## 第一部分：性能 + 成本（8 题）

### ⭐⭐⭐ Q1：LLM 调用延迟怎么优化？P99 怎么从 5s 降到 500ms？

**参考答案**：

**5 层优化**（按成本从低到高）：

#### L1：客户端缓存（0 成本）

```java
@Cacheable(value = "llm_cache", key = "#query", unless = "#result == null")
public String chat(String query) {
    return chatClient.prompt().user(query).call().content();
}
// 命中率 30-50% 时，调用量减半
```

#### L2：流式响应（用户体验）

```java
// 非流式：用户等 5s 才看到第一个字
// 流式：用户 0.5s 就看到第一个字
Flux<String> stream = chatClient.prompt()
    .user(query)
    .stream()
    .content();
```

**TTFT（Time to First Token）** 优化最关键。

#### L3：并发 + 批处理

```java
// 多个独立 LLM 调用并发
CompletableFuture<String> f1 = CompletableFuture.supplyAsync(() -> llm.call(q1));
CompletableFuture<String> f2 = CompletableFuture.supplyAsync(() -> llm.call(q2));
String r1 = f1.get();
String r2 = f2.get();
// 2s → 1s（不是 1+1=2）
```

#### L4：模型选择

```java
// 简单任务用小模型
if (task.isSimple()) {
    return deepseekChat("deepseek-chat");  // 1s
} else {
    return deepseekReasoner("deepseek-reasoner");  // 5s
}
```

#### L5：Prompt 优化

- 减少 token（不必要的 system prompt 砍掉）
- 用更小 context（裁剪输入）

**真实收益**：

| 优化 | 节省 |
|---|---|
| 缓存 50% | 50% 成本 |
| 流式 | 0% 成本，TTFT 提升 10x |
| 智能路由（小模型）| 70% 成本 |
| Prompt 优化 | 30% token |

**加分项**：
- 提到 **Anthropic Prompt Caching**——cache read 便宜 10x
- 提到 **DeepSeek cache read 便宜 50x**（0.0028/M vs 0.14/M）
- 提到 **TTFT 是用户感知关键**——不是总延迟

---

### ⭐⭐⭐ Q2：LLM 成本怎么算？怎么降低 70% 成本？

**参考答案**：

**2026 年主流 LLM 定价**：

| 模型 | Input ($/M) | Output ($/M) |
|---|---|---|
| **DeepSeek V3.2** | **0.14** | 0.28 |
| DeepSeek V4 Flash | 0.14 (miss) / 0.0028 (cache) | 0.28 |
| **GPT-5.4** | 2.50 | 10.00 |
| Claude Opus 4.6 | 5.00 (miss) / 0.50 (cache) | 25.00 |
| Gemini 2.5 Pro | 1.25 | 5.00 |
| Qwen3 Max | 0.40 | 1.20 |

**降低 70% 成本的 7 个方法**：

1. **智能路由**（简单任务用小模型）—— 节省 50%
2. **Prompt Caching**（重复 prefix 缓存）—— 节省 30-80%
3. **输入压缩**（裁剪不必要 context）—— 节省 30%
4. **流式 + 取消**（用户取消时不付后续 token）—— 节省 20%
5. **批处理**（合并多个请求）—— 节省 10%
6. **混合模型**（Hot data 用便宜模型）—— 节省 30%
7. **限流 + 配额**（防止滥用）—— 节省 20%

**真实账单算例**（50 人客服 Agent，月 50 万次调用）：

```
原方案（GPT-5.4）：
50万 × 2K input + 1K output = 1000M input + 500M output
= 1000M × 2.5 + 500M × 10 = 2500 + 5000 = $7500/月

优化后（DeepSeek + 缓存 + 路由）：
50万 × 1K (50% 缓存) × 0.14 + 50万 × 0.5K output × 0.28
+ 30% 简单任务用 V3.2
= 35 + 70 + 0 = $105/月

节省：$7500 → $105 = 节省 98.6%
```

**加分项**：
- 提到 **DeepSeek V4 Prompt Cache 便宜 50x**（0.0028 vs 0.14）
- 提到 **不同模型适合不同任务**——别全用 GPT
- 提到 **Token 计数是优化的前提**——不量就不知道省在哪

---

### ⭐⭐⭐ Q3：什么是 Prompt Caching？怎么启用？

**参考答案**：

**Prompt Caching = 把相同前缀的 prompt 在 LLM 厂商侧缓存**。

**工作原理**：

```
第 1 次：完整 prompt → 计费
第 2 次：相同前缀 → cache read 价格（10-50x 便宜）
```

**2026 年支持 Prompt Caching 的厂商**：

| 厂商 | 缓存粒度 | Cache Read 折扣 |
|---|---|---|
| **Anthropic Claude** | 1024 token block | 10x 便宜 |
| **OpenAI GPT-5.4** | 128 token block | 10x 便宜 |
| **DeepSeek V4** | 64 token block | **50x 便宜** |
| **Gemini 3.1** | 32 token block | 4x 便宜 |

**最佳实践**：

1. **把稳定的 system prompt + 上下文**放最前面
2. **把变化的 user query**放最后
3. **System prompt > 1024 token** 才能命中缓存

```java
// ✅ 正确：静态内容放前面
String prompt = """
    [SYSTEM] 你是 HR 助手，公司制度如下：
    ...（5000 字固定内容）
    
    [CONTEXT] 员工最近查询：
    ...（变化的上下文）
    
    [USER] 用户的具体问题：%s
""".formatted(userQuery);

// ❌ 错误：变化内容放最前，无法命中缓存
String prompt = """
    [USER] %s
    
    [SYSTEM] 你是 HR 助手...
""".formatted(userQuery);
```

**成本对比**（100K tokens prompt，重复 1000 次）：

| 方案 | 成本 |
|---|---|
| 不缓存 | 1000 × 100K × $0.14 = $14,000 |
| **DeepSeek 缓存** | $14,000 / 50 = **$280** |
| Anthropic 缓存 | $14,000 / 10 = $1,400 |

**加分项**：
- 提到 **Anthropic cache 5 分钟 TTL**——超时失效
- 提到 **OpenAI 自动缓存**——不用配
- 提到 **DeepSeek 50x 折扣是 2026 年最强**

---

### ⭐⭐⭐⭐ Q4：什么是 Token 路由？怎么用 Java 实现？

**参考答案**：

**Token 路由 = 根据任务复杂度选不同模型 + 不同参数**。

**核心思想**：**不是所有任务都需要 GPT-5.4**。

**路由策略**：

```
用户问题
    ↓
[分类器]  ← 用 LLM 判断（便宜模型）
    ↓
   ┌────┬────┬────┐
   ↓    ↓    ↓    ↓
闲聊  简单   复杂   推理
   ↓    ↓    ↓    ↓
V3.2 V3.2  GPT-5.4 R1
   $    $    $$    $$$
```

**Java 实现**：

```java
@Service
public class ModelRouter {

    @Autowired private DeepSeekChatClient cheap;
    @Autowired private DeepSeekReasonerClient reasoning;
    @Autowired private OpenAiChatClient premium;

    /**
     * 智能路由
     */
    public String route(String query) {
        // 1. 分类
        String complexity = classify(query);

        // 2. 选模型
        return switch (complexity) {
            case "CHITCHAT" -> cheap.call(query);     // 闲聊 → 便宜
            case "SIMPLE"   -> cheap.call(query);     // 简单问答
            case "COMPLEX"  -> premium.call(query);   // 复杂 → 旗舰
            case "REASONING" -> reasoning.call(query); // 数学/代码
            default -> cheap.call(query);
        };
    }

    private String classify(String query) {
        // 用便宜模型分类（或者规则）
        if (query.length() < 10 && !query.contains("?")) {
            return "CHITCHAT";
        }
        if (query.matches(".*(计算|证明|推导|代码|bug).*")) {
            return "REASONING";
        }
        return "COMPLEX";
    }
}
```

**效果**：

| 任务 | 路由前（全用 GPT-5.4）| 路由后 | 节省 |
|---|---|---|---|
| 闲聊 60% | $7500 | $0.14 × 30万 = $42 | 99% |
| 简单 25% | $3000 | $0.14 × 12.5万 = $18 | 99% |
| 复杂 12% | $1500 | $2.5 × 6万 = $150 | 90% |
| 推理 3% | $500 | $5.5 × 1.5万 = $83 | 83% |
| **总** | **$12,500** | **$293** | **97.6%** |

**加分项**：
- 提到 **DeepSeek R1 是推理专用**——复杂问题用它
- 提到 **BGE-M3 + 小 LLM 分类**——比 GPT-4 分类便宜 100x
- 提到 **动态路由**——根据 QPS 切换模型

---

### ⭐⭐⭐⭐ Q5：LLM 系统的限流怎么做？为什么要限流？

**参考答案**：

**为什么要限流？**

1. **成本控制**——防止 Token 滥用
2. **稳定性**——防止后端被打爆
3. **公平性**——防止单用户占用所有资源
4. **SLA 保障**——保障核心用户

**3 层限流**：

#### L1：网关层限流（粗粒度）

```java
// Nginx / Spring Cloud Gateway
@Bean
public RouteLocator routes(RouteLocatorBuilder builder) {
    return builder.routes()
        .route("llm_route", r -> r.path("/api/llm/**")
            .filters(f -> f.requestRateLimiter(c -> c
                .setRateLimiter(redisRateLimiter())
                .setKeyResolver(userKeyResolver())))
            .uri("http://llm-service"))
        .build();
}
// 每用户 100 QPS
```

#### L2：服务层限流（细粒度）

```java
// Resilience4j
@RateLimiter(name = "llm-service")
public String callLlm(String query) {
    return chatClient.prompt().user(query).call().content();
}
// 配置：每秒 1000 个请求
```

#### L3：模型层限流（厂商）

```java
// 不同模型不同配额
Map<String, Integer> quotas = Map.of(
    "deepseek-chat", 10000,     // 每分钟 10000 次
    "gpt-5.4", 1000,             // 每分钟 1000 次
    "claude-opus-4.6", 100       // 每分钟 100 次
);
```

**分布式限流**（Redis Lua 脚本）：

```lua
-- 滑动窗口限流
local key = KEYS[1]
local limit = tonumber(ARGV[1])
local window = tonumber(ARGV[2])
local current = redis.call('INCR', key)
if current == 1 then
    redis.call('EXPIRE', key, window)
end
if current > limit then
    return 0
end
return 1
```

**加分项**：
- 提到 **降级**——超限后 fallback 到本地模型
- 提到 **令牌桶 vs 漏桶**——限流算法选择
- 提到 **Redis 集群**——单 Redis 不够用

---

### ⭐⭐⭐ Q6：如何减少 LLM 调用的 Token 消耗？

**参考答案**：

**8 个技巧**：

| 技巧 | 节省 | 难度 |
|---|---|---|
| **1. Prompt 模板精简** | 20-30% | 简单 |
| **2. 删除冗余 system prompt** | 10-20% | 简单 |
| **3. Few-shot 选 1-2 个** | 30% | 中等 |
| **4. JSON 输出 vs 自由文本** | 10% | 简单 |
| **5. 压缩历史对话** | 50% | 中等 |
| **6. 用更小的 embedding**（1024→256）| 0% cost / 30% 内存 | 简单 |
| **7. 流式 + 用户取消** | 20% | 中等 |
| **8. 重复内容引用** | 30% | 中等 |

**详细代码**：

```java
// 1. 精简 system prompt
// ❌ 多余
String bad = "你是一个专业的、严谨的、可靠的助手...";
// ✅ 精简
String good = "你是 HR 助手";

// 2. 压缩历史
String compressedHistory = history.stream()
    .map(msg -> msg.role + ":" + msg.content.substring(0, Math.min(100, msg.content.length())))
    .collect(Collectors.joining("\n"));

// 3. 复用 system prompt（Prompt Cache）
String SYSTEM_PROMPT = "你是...";  // 静态
String userQuery = "...";
chatClient.prompt()
    .system(SYSTEM_PROMPT)
    .user(userQuery)
    .call();
```

**加分项**：
- 提到 **Token 计数是优化前提**——不量不知道
- 提到 **LangChain TokenCounter** 工具
- 提到 **OpenAI Tokenizer** 在线工具

---

### ⭐⭐⭐ Q7：LLM 服务的高可用怎么做？

**参考答案**：

**3 层高可用**：

#### L1：API Key 多 Key 轮询

```java
public class MultiKeyChatClient {
    private final List<String> apiKeys = List.of(
        System.getenv("DEEPSEEK_KEY_1"),
        System.getenv("DEEPSEEK_KEY_2"),
        System.getenv("DEEPSEEK_KEY_3")
    );
    private final AtomicInteger idx = new AtomicInteger(0);

    public String call(String query) {
        String key = apiKeys.get(idx.getAndIncrement() % apiKeys.size());
        return chatClient.withApiKey(key).prompt().user(query).call().content();
    }
}
```

#### L2：多厂商 Failover

```java
public class FailoverChatClient {
    public String call(String query) {
        // 1. 试 DeepSeek
        try {
            return deepseek.call(query);
        } catch (Exception e1) {
            // 2. 试通义千问
            try {
                return qwen.call(query);
            } catch (Exception e2) {
                // 3. 试 OpenAI
                return openai.call(query);
            }
        }
    }
}
```

#### L3：Resilience4j 熔断

```yaml
resilience4j:
  circuitbreaker:
    instances:
      llmService:
        slidingWindowSize: 100
        failureRateThreshold: 50
        waitDurationInOpenState: 30s
        permittedNumberOfCallsInHalfOpenState: 10
```

```java
@CircuitBreaker(name = "llmService", fallbackMethod = "fallback")
public String call(String query) {
    return deepseek.call(query);
}

private String fallback(String query, Throwable t) {
    // 1. 返回"服务繁忙，请稍后"
    // 2. 用本地模型（Ollama）
    return localModel.call(query);
}
```

**加分项**：
- 提到 **服务降级 3 级**：错误回复 / 简化回答 / 排队提示
- 提到 **流量整形**——突发流量缓冲
- 提到 **Vendor lock-in 风险**——多厂商准备

---

### ⭐⭐⭐⭐ Q8：LLM 推理优化有哪些高级技巧？

**参考答案**：

**7 个高级技巧**：

#### 1. Speculative Decoding（投机解码）

- 用小模型先草拟答案，旗舰模型验证
- **速度提升 2-3x**（不损质量）

#### 2. Prefix Caching

- 共享前缀只算 1 次
- vLLM / SGLang 支持

#### 3. Continuous Batching

- 多个请求 batch 推理
- **吞吐提升 10-20x**

#### 4. Quantization

- FP32 → INT8 / FP16
- **显存省 2-4x**

#### 5. KV Cache

- Transformer 注意力缓存
- **长 prompt 提速 5-10x**

#### 6. PagedAttention（vLLM 核心）

- 像 OS 内存管理一样管 KV cache
- **吞吐提升 24x**

#### 7. Model Parallelism

- 张量并行 + 流水线并行
- **单卡跑不下大模型时用**

**实战推荐**：

```
    小团队 / 中等 QPS
         ↓
    OpenAI 兼容 API + DeepSeek（vLLM 部署）
         ↓
    大规模 / 成本敏感
         ↓
    自建 vLLM + DeepSeek V3.2 / Llama 3.3
```

**加分项**：
- 提到 **vLLM 是 2026 推理引擎事实标准**
- 提到 **SGLang**（更适合 Agent，多轮对话优化）
- 提到 **TensorRT-LLM**（NVIDIA 优化）

---

## 第二部分：可观测性（6 题）

### ⭐⭐⭐ Q9：LLM 系统的可观测性指标有哪些？

**参考答案**：

**4 大指标**（详见深挖版 7）：

#### 1. LLM 性能指标

| 指标 | 含义 | 健康值 |
|---|---|---|
| **TTFT** | Time to First Token | < 500ms |
| **TPS** | Tokens Per Second | > 30 |
| **Total Latency** | 总延迟 | < 5s |
| **P99 Latency** | 99 分位延迟 | < 10s |

#### 2. LLM 质量指标

| 指标 | 含义 | 工具 |
|---|---|---|
| **Faithfulness** | 答案是否基于上下文 | Ragas |
| **Answer Relevancy** | 答案是否切题 | Ragas |
| **Hallucination Rate** | 幻觉率 | DeepEval |
| **Context Recall** | 召回了多少相关 | Ragas |

#### 3. LLM 成本指标

| 指标 | 含义 |
|---|---|
| **Token / Request** | 每次请求平均 token |
| **Cost / Request** | 每次请求成本 |
| **Cost / User** | 每用户成本 |

#### 4. 业务指标

- **采纳率**——用户是否用了建议
- **点赞率**——用户反馈
- **转化率**——推荐了商品是否买

**加分项**：
- 提到 **OTel GenAI 语义约定**（1.35+ 规范）
- 提到 **Langfuse**（开源 LLM 监控）
- 提到 **Braintrust**（生产监控）

---

### ⭐⭐⭐ Q10：LLM 调用怎么埋点？OpenTelemetry 怎么集成？

**参考答案**：

**Spring AI 1.0 + OpenTelemetry 集成**：

```java
@Bean
public ObservationRegistry observationRegistry() {
    return ObservationRegistry.create();
}

@Bean
public ChatClient chatClient(ChatModel model) {
    return ChatClient.builder(model)
        .observationRegistry(observationRegistry)
        .build();
}
```

**自动埋点**：
- **LLM 调用**（prompt、response、tokens、latency）
- **Tool 调用**（name、args、result、error）
- **Vector Search**（query、result count、latency）
- **Agent Loop**（state、iterations、cost）

**OTel GenAI 语义约定字段**：

```java
Span span = tracer.spanBuilder("llm.call").startSpan();
span.setAttribute("gen_ai.system", "openai");
span.setAttribute("gen_ai.request.model", "gpt-5.4");
span.setAttribute("gen_ai.usage.input_tokens", 1500);
span.setAttribute("gen_ai.usage.output_tokens", 800);
span.setAttribute("gen_ai.request.temperature", 0.7);
span.setAttribute("gen_ai.response.finish_reason", "stop");
```

**导出到后端**：

```yaml
# application.yml
management:
  otlp:
    tracing:
      endpoint: http://jaeger:4318/v1/traces
  observations:
    annotations:
      enabled: true
```

**加分项**：
- 提到 **Langfuse**（LLM 专用，OTel 兼容）
- 提到 **Braintrust**（生产监控）
- 提到 **Prompt 也要存**——Langfuse 自动存

---

### ⭐⭐⭐⭐ Q11：怎么排查 LLM 系统的故障？给个真实案例

**参考答案**：

**真实案例**（深挖版 7 详细）：某 Agent 上线后**响应变慢 5 倍**。

**排查步骤**：

#### 步骤 1：看大盘（30 秒）

- 看 Grafana：哪个 P99 涨了？
- **发现**：LLM 调用延迟涨 5 倍（500ms → 2.5s）

#### 步骤 2：下钻（2 分钟）

- 看分布式 Trace：哪个 Span 慢？
- **发现**：Vector Search Span 涨 3 倍（10ms → 30ms）

#### 步骤 3：查根因（5 分钟）

- 查向量库的 metrics
- **发现**：HNSW 索引被刷爆（2000 个 segment）

**根因**：
- 新数据持续灌入，但 HNSW 索引是后台异步构建
- 索引没建完前查询走 fallback（暴力搜索）

#### 步骤 4：解决

- **短期**：手动 trigger 索引构建
- **长期**：加 segment 上限 + 自动合并

**排查工具链**：

| 工具 | 用途 |
|---|---|
| **Jaeger / Tempo** | 分布式 Trace |
| **Grafana** | 指标看板 |
| **Langfuse** | LLM 调用分析 |
| **Loki** | 日志聚合 |
| **Prometheus** | 指标抓取 |

**加分项**：
- 提到 **Google SRE 故障排查三段**：症状 → 根因 → 修复
- 提到 **Runbook**——每个常见故障的处理手册
- 提到 **混沌工程**——故意制造故障

---

### ⭐⭐⭐ Q12：什么是 LLM-as-a-Judge？准确性如何？

**参考答案**：

**LLM-as-a-Judge = 用 LLM 评估 LLM 输出**。

**典型场景**：

```java
// 评估答案质量
String prompt = """
    请对下面这个 RAG 答案打分（0-1）：
    
    问题：%s
    上下文：%s
    答案：%s
    
    评分标准：
    - 0.0-0.3：与上下文不符（幻觉）
    - 0.4-0.6：部分基于上下文
    - 0.7-0.9：完全基于上下文
    - 1.0：完美
""".formatted(question, context, answer);

String score = judgeClient.prompt().user(prompt).call().content();
```

**准确性**：

| 任务 | GPT-4 Judge 准确率 | 跟人类一致性 |
|---|---|---|
| **答案质量** | 80% | 85% |
| **RAG 评估** | 75% | 80% |
| **代码评估** | 70% | 75% |

**问题**：

1. **位置偏差**——喜欢前面的答案
2. **长度偏差**——喜欢更长的
3. **自利偏差**——喜欢自己生成的
4. **格式敏感**——prompt 变一点分数大变

**最佳实践**：

- **多 LLM 投票**（3 个不同模型评分，取平均）
- **盲评**（不让 Judge 知道答案来源）
- **校准**（用人类标注的小集合校准）

**加分项**：
- 提到 **Anthropic 论文**——LLM-as-Judge 跟人类一致性 80%
- 提到 **位置偏差可以随机化答案顺序解决**
- 提到 **DeepEval / Ragas 内置 LLM Judge**

---

### ⭐⭐⭐ Q13：LLM 系统的告警怎么设计？

**参考答案**：

**4 级告警**：

#### P0 紧急（电话告警）

- LLM 服务完全不可用（错误率 > 50%）
- 核心业务完全中断

#### P1 严重（短信 + 钉钉）

- P99 延迟 > 10s
- 错误率 > 10%
- Token 成本 1 小时翻倍

#### P2 警告（飞书 / 邮件）

- P95 延迟 > 5s
- 错误率 > 5%
- 缓存命中率 < 60%

#### P3 提醒（日报）

- 性能指标下降 20%
- 用户反馈负面增加

**Grafana 告警规则示例**：

```yaml
- alert: LLMHighErrorRate
  expr: rate(llm_requests_total{status="error"}[5m]) / rate(llm_requests_total[5m]) > 0.1
  for: 5m
  labels:
    severity: P1
  annotations:
    summary: "LLM 错误率过高"
    description: "过去 5 分钟错误率 {{ $value }}%"

- alert: LLMHighLatency
  expr: histogram_quantile(0.99, llm_request_duration_seconds_bucket) > 10
  for: 5m
  labels:
    severity: P1
```

**加分项**：
- 提到 **告警疲劳**——告警太多等于没告警
- 提到 **错误预算**（SRE 概念）
- 提到 **值班轮换 + 升级机制**

---

### ⭐⭐⭐⭐ Q14：什么是 SLO？LLM 系统的 SLO 怎么定？

**参考答案**：

**SLO（Service Level Objective，服务等级目标）= 可量化的服务质量承诺**。

**LLM 系统的 4 个核心 SLO**：

#### 1. 可用性 SLO

```
99.9% 可用 = 每月最多 43 分钟不可用
99.99% 可用 = 每月最多 4.3 分钟不可用
```

#### 2. 延迟 SLO

```
P50 < 1s
P95 < 3s
P99 < 5s
```

#### 3. 准确性 SLO

```
答案准确率 > 90%
幻觉率 < 5%
```

#### 4. 成本 SLO

```
每用户每天成本 < ¥1
每月总成本 < ¥100,000
```

**错误预算（Error Budget）**：

```
月度错误预算 = (1 - 99.9%) × 30 × 24 × 60 = 43 分钟
当月用了 30 分钟，还剩 13 分钟
→ 还可以承受 13 分钟的不可用
→ 超出后停止新功能上线，专注稳定
```

**实战 SLO 定义**：

```yaml
slo:
  availability:
    target: 99.9%
    window: 30d
  latency:
    p50: 1s
    p95: 3s
    p99: 5s
  quality:
    accuracy: 90%
    hallucination_rate: 5%
```

**加分项**：
- 提到 **Google SRE 圣经**——SLO 比 SLA 重要
- 提到 **Burn Rate 告警**（2% / 14.4 倍预算消耗率）
- 提到 **多窗口多阈值**告警

---

## 第三部分：安全 + 多租户（6 题）

### ⭐⭐⭐ Q15：什么是 Prompt Injection？怎么防御？

**参考答案**：

**详见专题 1 Q8**——2 大类型 + 5 层防御。

**补充：企业级防御**：

1. **输入层**——正则 + 黑名单
2. **Prompt 层**——指令加固 + Delimiter
3. **输出层**——检查 LLM 行为
4. **架构层**——最小权限
5. **运营层**——红队测试

**加分项**：
- 提到 **Anthropic Constitutional AI**
- 提到 **Llama Guard / ShieldGemma**（输出过滤模型）

---

### ⭐⭐⭐ Q16：什么是 OWASP LLM Top 10？

**参考答案**：

**OWASP LLM Top 10（2025 版）** = LLM 应用最常见的 10 大安全风险。

| # | 风险 | 例子 |
|---|---|---|
| **LLM01** | Prompt Injection | 攻击者注入恶意指令 |
| **LLM02** | 敏感信息泄露 | 训练数据 / 上下文泄露 |
| **LLM03** | 训练数据投毒 | 投毒 RAG 知识库 |
| **LLM04** | 模型 DoS | 长 prompt 耗资源 |
| **LLM05** | 供应链漏洞 | 第三方模型被污染 |
| **LLM06** | 不安全输出处理 | XSS / SQL 注入 |
| **LLM07** | 过度代理 | Agent 权限过大 |
| **LLM08** | 过度依赖 | 用户信 LLM 太多 |
| **LLM09** | 模型盗窃 | API 被滥用 |
| **LLM10** | 模型混乱 | 误用模型能力 |

**加分项**：
- 提到 **OWASP 2025 版**有调整
- 提到 **NIST AI RMF**——AI 风险管理框架

---

### ⭐⭐⭐ Q17：多租户 LLM 系统怎么设计？怎么防止数据串号？

**参考答案**：

**3 层隔离**：

#### L1：API 网关层（认证 + 租户识别）

```java
public String chat(HttpServletRequest req, String query) {
    String tenantId = jwtParser.getTenantId(req);
    // 每个请求都带租户 ID
    return llmService.call(tenantId, query);
}
```

#### L2：数据层（行级 / Collection 级隔离）

```java
// 向量库过滤
SearchRequest req = SearchRequest.builder()
    .query(query)
    .filterExpression("tenant_id == '" + tenantId + "'")
    .build();
List<Document> results = vectorStore.search(req);

// 关系数据库
@PreAuthorize("hasPermission(#customer, 'read')")
public Customer findById(Long id) {...}
```

#### L3：LLM Prompt 层（强制约束）

```java
String systemPrompt = """
    你是 %s 公司的助手。
    严格只回答 %s 业务范围内的问题。
    不要提及任何其他公司的数据。
""".formatted(tenantName, tenantBusiness);
```

**完整的多租户 RAG 架构**：

```
用户 A (租户 X)    用户 B (租户 Y)
    ↓                ↓
[认证]  →  tenant_id  ←  [认证]
    ↓                ↓
   [RAG 检索] tenant_id == X    [RAG 检索] tenant_id == Y
    ↓                ↓
   [LLM] prompt 强调 X 公司    [LLM] prompt 强调 Y 公司
    ↓                ↓
   回答 A 的问题       回答 B 的问题
```

**加分项**：
- 提到 **Tenant Context 传播**——OpenTelemetry Baggage
- 提到 **审计日志必须含 tenant_id**
- 提到 **RAG 检索必须带 tenant_id 过滤**

---

### ⭐⭐⭐⭐ Q18：什么是 Tool Use 沙箱？为什么需要？

**参考答案**：

**Tool Use 沙箱 = 在隔离环境执行 LLM 调用的工具**。

**为什么需要？**

真实事故：
- 2024 年某 Agent 被 Prompt Injection → **自动转账 100 万**
- 2024 年某 Agent 执行 Python 代码 → **删了数据库**

**4 层防御**：

```
L1 工具白名单（OPA）
    ↓
L2 参数校验（防注入）
    ↓
L3 隔离执行（Docker / gVisor / Firecracker）
    ↓
L4 高危操作审批（人机协同）
```

**实战代码**（Docker 沙箱）：

```java
@Tool(description = "执行 Python 代码（沙箱环境，无网络）")
public String executePython(String code) {
    // 1. 启动隔离容器
    String containerId = dockerClient.createContainer("python:3.11")
        .withNetworkMode("none")        // 关键：无网络
        .withMemoryLimit(256 * 1024 * 1024)  // 256MB
        .withCpuLimit(0.5)
        .withReadonlyRootFs()
        .exec();

    // 2. 复制代码
    dockerClient.copyToContainer(containerId, code.getBytes(), "/tmp/code.py");

    // 3. 执行（30s 超时）
    ExecResult result = dockerClient.exec(containerId, "python", "/tmp/code.py")
        .withTimeout(30)
        .exec();

    // 4. 清理
    dockerClient.removeContainer(containerId);
    return result.getOutput();
}
```

**gVisor 配置**（更安全）：

```yaml
apiVersion: node.k8s.io/v1
kind: RuntimeClass
metadata:
  name: gvisor
handler: runsc
```

**加分项**：
- 提到 **Firecracker**（AWS Lambda 用的微 VM）
- 提到 **gVisor**（Google 用的）
- 提到 **WASM**（WebAssembly 沙箱，2026 趋势）

---

### ⭐⭐⭐⭐ Q19：怎么实现"高危操作必须人工审批"？

**参考答案**：

**设计模式**：

```java
@Tool(description = "转账（需要人工审批）")
public String transfer(
    @ToolParam(description = "收款账户") String toAccount,
    @ToolParam(description = "金额（元）") BigDecimal amount
) {
    if (amount.compareTo(new BigDecimal("10000")) > 0) {
        // 1. 创建审批单
        String approvalId = approvalService.create(
            UserContext.get().userId(),
            "transfer",
            Map.of("to", toAccount, "amount", amount)
        );

        // 2. 等审批（不真转账）
        return "已创建转账审批 " + approvalId + "，等待 CFO 审批";
    }

    // 小额直接执行
    return transferService.execute(toAccount, amount);
}
```

**工作流**：

```
LLM 想调 transfer(amount=50000)
    ↓
1. Agent 不直接调，先 createApproval
    ↓
2. 推送通知给 CFO（飞书/钉钉）
    ↓
3. CFO 点"同意"按钮
    ↓
4. 回调 Agent，继续执行 transfer
    ↓
5. 调实际转账 API
```

**Spring AI Graph 实现**：

```java
StateGraph graph = new StateGraph("approval-workflow");
graph.addNode("create_approval", node(state -> {
    String approvalId = approvalService.create(...);
    return Map.of("approval_id", approvalId);
}));
graph.addNode("wait_for_approval", node(state -> {
    approvalService.waitFor(state.get("approval_id"));
    return Map.of("approved", true);
}));
graph.addNode("execute_transfer", node(state -> {
    transferService.execute(...);
    return Map.of("status", "done");
}));
```

**加分项**：
- 提到 **审批超时**——超过 24h 自动撤销
- 提到 **审批人轮换**——防腐败
- 提到 **金额分档**——不同档不同审批人

---

### ⭐⭐⭐⭐ Q20：GDPR / 数据合规对 LLM 系统有什么要求？

**参考答案**：

**GDPR 4 大要求**：

1. **知情同意**——用户知道数据被用
2. **数据删除权**（"被遗忘权"）——用户能要求删数据
3. **数据可携**——用户能导出数据
4. **目的限制**——数据只能用于声明的目的

**LLM 系统具体要求**：

| 要求 | LLM 系统实现 |
|---|---|
| **数据最小化** | 不存不必要的数据 |
| **加密存储** | 落盘加密（AES-256）|
| **加密传输** | HTTPS / TLS 1.3 |
| **访问控制** | RBAC / ABAC |
| **审计日志** | 所有访问记录 |
| **数据删除** | 用户能删对话、记忆、训练数据 |
| **跨境传输** | 受限地区本地化 |

**实战代码（数据删除）**：

```java
@Service
public class GdprService {

    /**
     * 响应用户"被遗忘权"请求
     */
    public void forgetUser(String userId) {
        // 1. 删对话历史
        chatHistoryRepo.deleteByUserId(userId);

        // 2. 删向量库中的用户文档
        vectorStore.delete(filterExpression("user_id == '" + userId + "'"));

        // 3. 删用户记忆
        memoryStore.deleteByUserId(userId);

        // 4. 删日志（保留脱敏）
        logArchive.anonymizeUser(userId);

        // 5. 记录审计（这次删除操作本身）
        auditLog.record("gdpr_forget", userId, Instant.now());
    }
}
```

**加分项**：
- 提到 **CCPA**（加州）类似 GDPR
- 提到 **中国《个人信息保护法》PIPL**
- 提到 **数据本地化**——中国数据不出境

---

## 第四部分：综合（5 题）

### ⭐⭐⭐⭐ Q21：给一个"从 0 到 1 设计企业级 Agent 系统"的方案

**参考答案**：

**8 个步骤**：

#### 1. 需求分析

- 业务场景：客服 / 销售 / 研发 / 知识库
- 用户数 / QPS / SLA
- 数据敏感度 / 合规要求

#### 2. 选型

| 决策 | 选择 |
|---|---|
| 框架 | Spring AI 1.0 / LangChain4j |
| LLM | DeepSeek V3.2 / GPT-5.4 |
| Embedding | BGE-M3 |
| 向量库 | Milvus / Qdrant |
| 监控 | Langfuse + Prometheus |

#### 3. 架构设计

```
API Gateway
    ↓
Agent Service (Spring AI)
    ↓
[LLM] [Tools] [Memory] [RAG]
    ↓
[PostgreSQL] [Milvus] [Redis] [Object Storage]
    ↓
[Observability: Trace/Metric/Log]
```

#### 4. 数据流设计

- 文档摄入（离线 pipeline）
- 在线检索 + 生成
- 反馈收集

#### 5. 安全设计

- 认证（OAuth2 / JWT）
- 多租户隔离
- 工具沙箱
- 高危操作审批
- 审计日志

#### 6. 可观测性

- Trace（OTel）
- Metric（Prometheus）
- Log（Loki）
- LLM 评估（DeepEval）

#### 7. 灰度发布

- A/B 测试
- Canary 发布
- 配置热更新

#### 8. 持续优化

- RAG 调优
- Prompt 优化
- 成本优化
- 评估体系

**加分项**：
- 提到 **分阶段上线**——内部 → 小流量 → 全量
- 提到 **降级方案**——LLM 挂了怎么办
- 提到 **灾备**——多 region

---

### ⭐⭐⭐⭐ Q22：LLM 系统的"扩展性"怎么设计？

**参考答案**：

**4 个维度**：

#### 1. 流量扩展

- **水平扩展**——Agent Pod 多实例
- **限流**——防突发流量
- **熔断**——防雪崩
- **异步队列**（Kafka）——削峰

#### 2. 数据扩展

- **向量库分片**——按 tenant 分
- **PostgreSQL 读写分离**——主从
- **冷热数据分层**——老数据冷存

#### 3. 模型扩展

- **多模型路由**——按任务选
- **本地 + 云端混合**——本地保底
- **厂商多 Key**——防单 Key 限流

#### 4. 团队扩展

- **微服务化**——不同团队管不同 Agent
- **API 标准化**——Agent ↔ Agent 用 A2A

**加分项**：
- 提到 **Saga 模式**——跨服务事务
- 提到 **CQRS**——读写分离架构
- 提到 **多 region 部署**

---

### ⭐⭐⭐⭐ Q23：LLM 系统的"灾难恢复"怎么设计？

**参考答案**：

**5 个 RTO/RPO 目标**：

| 等级 | RTO | RPO | 适合 |
|---|---|---|---|
| **L1** | 1h | 0 | 核心业务 |
| **L2** | 4h | 1h | 重要业务 |
| **L3** | 24h | 24h | 一般业务 |

**LLM 系统 DR 方案**：

| 故障 | 方案 | RTO |
|---|---|---|
| **LLM 厂商挂了** | Failover 到备用厂商 | < 1min |
| **向量库挂了** | 切到只读备份 | < 5min |
| **DB 挂了** | 切主从 | < 1min |
| **整个 Region 挂了** | 切 Region | < 30min |
| **配置丢失** | Git 仓库恢复 | < 10min |
| **Prompt 错误** | Git 回滚 + Canary | < 5min |

**备份策略**：

```yaml
# 向量库每日全量备份
backup:
  vector_db:
    schedule: "0 3 * * *"  # 每天 3 点
    retention: 30 天
    storage: S3

  # PostgreSQL WAL 归档
  postgres:
    schedule: 实时
    retention: 7 天
    storage: S3
```

**加分项**：
- 提到 **Chaos Engineering**（Chaos Monkey）
- 提到 **Drill**（每季度演练）
- 提到 **多 Region 多活**（vs 主备）

---

### ⭐⭐⭐⭐ Q24：LLM 系统的"技术债"怎么识别和还？

**参考答案**：

**5 大技术债**：

#### 1. Prompt 散落

**症状**：Prompt 写在代码里 / 数据库 / 文件
**修复**：用 Prompt Registry 集中管理 + 版本控制

#### 2. Token 浪费

**症状**：每次调用 5K token
**修复**：用 Token 计数 + 优化 Prompt

#### 3. 没有评估

**症状**：不知道准确率
**修复**：DeepEval 自动化 + Golden Test Set

#### 4. 没有监控

**症状**：出问题了才发现
**修复**：Langfuse + Prometheus

#### 5. 没有成本归因

**症状**：不知道钱花在哪
**修复**：按 tenant / user / feature 拆成本

**还债节奏**：

| 时间 | 事项 |
|---|---|
| 每周 | 处理 P0 报警 |
| 每月 | 优化 1-2 个高频 Prompt |
| 每季度 | 性能优化 1 次 |
| 每年 | 架构升级 1 次 |

**加分项**：
- 提到 **"先活下去，再还债"**——业务稳定后再优化
- 提到 **Martin Fowler 技术债四象限**
- 提到 **还债要量化收益**

---

### ⭐⭐⭐⭐⭐ Q25：AI Agent 系统的"未来 3 年"会怎么演化？

**参考答案**：

**8 大趋势**（基于 2026 年最新研究）：

#### 1. Agent 框架大一统

- LangGraph / Spring AI Graph / Dify 趋同
- **MCP 协议成事实标准**（2025-2026）
- **A2A 协议快速成熟**（2026-2027）

#### 2. Agent 推理能力突破

- DeepSeek R1 / o1 推理范式普及
- **Agent 自我反思成为标配**
- **长任务（小时级）能完成**

#### 3. Agent 操作系统化

- **Agent OS** 概念兴起
- 类比"Linux for Agents"
- **多 Agent 协作 = 默认配置**

#### 4. AI 评估产业化

- **Ragas / DeepEval 标准化**
- **企业 AI 评估部门**（类比 QA 部门）
- **监管要求可解释性**

#### 5. Edge AI Agent

- **端侧 Agent**（手机 / IoT / 车）
- 模型量化 + 小模型
- **离线 + 隐私优先**

#### 6. AI 安全军备竞赛

- **红队**成为标配
- **AI 对 AI 的攻防**（用 LLM 攻击 LLM）
- **新的 OWASP LLM Top 10**

#### 7. 企业 AI 基础设施

- **LLMOps** 平台化（类比 DevOps）
- **AI Governance**（合规、审计、可观测）
- **AI FinOps**（成本管理）

#### 8. Java 栈的崛起

- **Spring AI 成为企业 Java AI 标准**
- **企业内部 Java Agent > Python**
- **LangChain4j + Spring AI 主导**

**对 Java 工程师的建议**：

1. **学 Spring AI 1.0**——未来 3 年 Java 栈最稳的选择
2. **学企业级工程**——不学 LLM API 也会被淘汰
3. **学多 Agent 协作**——A2A / MCP 是未来

**加分项**：
- 提到 **Sam Altman 的 "Agentic Web" 概念**
- 提到 **OpenAI 的 AGI 路线图**
- 提到 **Google Gemini Agent Foundry**

---

## 写在最后

**这份 25 题覆盖了企业级工程的所有核心问题**——从性能到成本，从可观测到安全。

**给你的 3 条面试建议**：

1. **80% 的题有"标准答案"**——背熟基础 + 项目经验
2. **20% 的题是开放题**——展示你的深度思考
3. **Java 视角是杀手锏**——能讲 Spring AI 1.0 + 企业级工程，比 Python 候选人更有差异化

**配合前 3 篇（专题 1/2/3）**——你已经覆盖了企业级 Agent 面试的所有核心。

---

