# 深挖版 10：企业级向量数据库——Milvus / Qdrant / Weaviate / pgvector 完整实战

> 日期：2026-06-10
> 配套基础版 + 深挖版 1/2/3/4/5/6/7/8/9
> 适合：负责企业级 RAG / 语义搜索系统的架构师

---

## 写在前面：为什么"选对向量库"是 RAG 系统的"地基"？

**先说个真实事故**。

2025 年某公司用 **pgvector** 跑企业知识库 RAG，3 个月后数据量从 10 万涨到 1000 万。
- 召回延迟从 50ms 涨到 **8 秒**
- 准确率从 90% 掉到 **65%**（HNSW 索引被刷爆）
- 最后不得不迁移到 **Milvus 集群**

**为什么？**——**pgvector 适合 < 100 万向量的轻量场景**；**1000 万级以上必须用专门的向量数据库**。

**2026 年企业级向量库选型，已经不是"选 Milvus 还是 Qdrant"，而是"哪几个组合用"**：

| 场景 | 推荐 | 理由 |
|---|---|---|
| **个人项目 / 原型** | Chroma / LanceDB | 零配置，嵌入式 |
| **小团队 / 100 万向量以内** | **pgvector** | 跟 PG 共生，少一个组件 |
| **中等规模 / 1000 万向量** | Qdrant / Weaviate | 性能好，单机够用 |
| **大规模 / 亿级向量** | **Milvus 集群** | 分布式、HNSW 成熟 |
| **超大规模 / 10 亿+** | Milvus + GPU / 阿里云 ADS | 分布式 + 硬件加速 |
| **云原生 SaaS** | Pinecone / Zilliz Cloud | 托管，省运维 |

**这一篇我把向量数据库讲透**——从索引原理、选型对比、Milvus 集群实战、调优技巧、混合检索。

---

## 第一部分：向量检索核心原理

### 1.1 相似度度量

**4 种常见相似度算法**：

| 算法 | 公式 | 特点 | 适合 |
|---|---|---|---|
| **Cosine** | `(A·B) / (||A|| * ||B||)` | 忽略向量长度 | 文本 Embedding（最常用）|
| **Euclidean (L2)** | `sqrt(Σ(Ai-Bi)²)` | 考虑距离 | 图像 Embedding |
| **Dot Product** | `A·B` | 速度快 | 归一化向量 |
| **Manhattan (L1)** | `Σ|Ai-Bi|` | 维度灾难 | 很少用 |

**经验值**：**文本场景用 Cosine**，**图像/音频用 Euclidean**。

### 1.2 索引算法——核心中的核心

**为什么需要索引？** 100 万向量暴力搜索 = 100 万 × 1024 维 = 10 亿次运算。**10 秒**。

**索引 = 用空间换时间，预计算"近邻关系"**。

#### 索引 1：Flat（暴力搜索）

- **原理**：跟每个向量算距离
- **复杂度**：O(N·D)
- **准确率**：100%
- **速度**：最慢
- **适合**：< 1 万向量 / 评测 baseline

#### 索引 2：IVF（Inverted File Index）

- **原理**：用 K-Means 聚类，分成 N 个桶，**只搜最近的几个桶**
- **参数**：`nlist`（桶数量，建议 `4 × sqrt(N)`）、`nprobe`（搜几个桶）
- **速度**：10-100x 加速
- **准确率**：95-99%
- **适合**：100 万 - 1 亿向量

#### 索引 3：HNSW（Hierarchical Navigable Small World）⭐⭐⭐⭐⭐

- **原理**：构建多层图，**像跳表一样搜**
- **参数**：`M`（每个节点的边数）、`efConstruction`（建图时的搜索深度）、`ef`（查询时搜索深度）
- **速度**：100-1000x 加速
- **准确率**：> 99%
- **适合**：**生产首选**（Milvus / Qdrant / Weaviate 默认）

#### 索引 4：ANNOY（Approximate Nearest Neighbors Oh Yeah）

- **原理**：用多棵随机投影树
- **特点**：Spotify 开源、**只读**（不能增量添加）
- **速度**：中等
- **适合**：静态数据集

#### 索引 5：ScaNN（Google）

- **原理**：向量量化 + 改进的 IVF
- **特点**：Google 开源，**SOTA 性能**
- **适合**：海量数据

### 1.3 量化（Quantization）

**问题**：1000 万个 1024 维 float32 向量 = **40 GB**。

**解法**：量化（用更少的 bit 存）

| 量化方法 | 压缩比 | 内存 | 准确率损失 |
|---|---|---|---|
| **FP32** | 1x | 4 bytes/dim | 0% |
| **FP16** | 2x | 2 bytes/dim | < 0.5% |
| **INT8** | 4x | 1 byte/dim | < 2% |
| **Binary** | 32x | 1 bit/dim | 3-5% |
| **PQ（乘积量化）** | 16-32x | 0.1-0.2 bytes/dim | 1-3% |

**生产推荐**：**FP32（在线） + INT8（建索引） + PQ（备份）** 三级量化。

### 1.4 召回-精排两阶段

```
向量库召回 top 100-1000（IVF/HNSW 索引，~50ms）
   ↓
Cross-Encoder Rerank 取 top 10（~300ms，详见深挖版 4）
```

**两阶段的精髓**：**用便宜的 Bi-Encoder 召回（速度快但粗），用贵的 Cross-Encoder 精排（速度慢但准）**。

---

## 第二部分：4 大向量库完整对比

### 2.1 综合对比

| 维度 | **Milvus** | **Qdrant** | **Weaviate** | **pgvector** |
|---|---|---|---|---|
| **架构** | 分布式（云原生）| 单机 + 集群 | 单机 + 集群 | PG 扩展 |
| **语言** | Go + C++ | Rust | Go | C（PG 扩展）|
| **索引** | HNSW / IVF / ANNOY / ScaNN | HNSW | HNSW | HNSW / IVF |
| **最大规模** | **100 亿+** | 10 亿 | 10 亿 | 1000 万 |
| **延迟 P99** | < 10ms | < 5ms | < 20ms | < 50ms |
| **吞吐量** | **10K+ QPS** | 5K QPS | 3K QPS | 500 QPS |
| **混合检索** | ✅（BM25 + 向量）| ✅ | ✅ | ✅（tsvector）|
| **过滤** | ✅（标量 + 向量联合）| ✅（payload 过滤极强）| ✅ | ✅ |
| **多租户** | ✅（Database/Collection）| ✅ | ✅ | ✅（schema）|
| **运维** | 复杂（K8s 友好）| 中等 | 中等 | 简单（PG 一把梭）|
| **License** | Apache 2.0 | Apache 2.0 | BSD-3 | PostgreSQL License |
| **2026 现状** | **生产首选** | 性能最强 | 文档最好 | 简单场景首选 |
| **Spring AI 集成** | ✅ | ✅ | ✅ | ✅ |

### 2.2 选型决策树

```
你的数据量？
    │
    ├── < 10 万向量
    │   └── 任何都行（甚至 Chroma / LanceDB）
    │
    ├── 10 万 - 100 万
    │   │
    │   ├── 已有 PG → pgvector（少一个组件）
    │   ├── 性能敏感 → Qdrant
    │   └── 默认选择 → Milvus 单机
    │
    ├── 100 万 - 1000 万
    │   │
    │   ├── 性能敏感 → Qdrant
    │   ├── 多租户 / 分片 → Milvus
    │   └── 已用 ES → Weaviate
    │
    └── > 1000 万
        │
        ├── 自建集群 → Milvus（事实标准）
        ├── 不想运维 → Zilliz Cloud / Pinecone
        └── 国内合规 → 阿里云 ADS / 腾讯云 VectorDB
```

### 2.3 真实案例（2026 年企业部署数据）

| 公司 | 规模 | 选型 | QPS | P99 延迟 |
|---|---|---|---|---|
| **某电商** | 5 亿商品向量 | Milvus 集群（10 节点）| 8K | 12ms |
| **某金融** | 5000 万文档向量 | Qdrant 集群 | 3K | 6ms |
| **某客服** | 100 万 FAQ 向量 | pgvector | 200 | 30ms |
| **某法律** | 2000 万判例 | Milvus + GPU | 2K | 15ms |
| **某医疗** | 800 万文献 | Weaviate | 1K | 25ms |

---

## 第三部分：Milvus 完整实战

### 3.1 Milvus 2.x 架构

```
┌────────────────────────────────────────────┐
│ Milvus Standalone / Cluster                │
│                                            │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐ │
│  │  Proxy   │  │  Proxy   │  │  Proxy   │ │  ← 接入层
│  └──────────┘  └──────────┘  └──────────┘ │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐ │
│  │ Query    │  │ Query    │  │ Query    │ │  ← 查询节点（读）
│  └──────────┘  └──────────┘  └──────────┘ │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐ │
│  │ Data     │  │ Data     │  │ Data     │ │  ← 数据节点（写）
│  └──────────┘  └──────────┘  └──────────┘ │
│  ┌──────────────────────────────────────┐  │
│  │  Meta Storage (etcd)                 │  │  ← 元数据
│  └──────────────────────────────────────┘  │
│  ┌──────────────────────────────────────┐  │
│  │  Object Storage (MinIO / S3)         │  │  ← 向量文件
│  └──────────────────────────────────────┘  │
└────────────────────────────────────────────┘
```

**Standalone 模式**：单节点，开发用
**Cluster 模式**：分布式，生产用

### 3.2 Milvus Cluster 部署（K8s + Helm）

```bash
# 1. 添加 Helm 仓库
helm repo add milvus https://milvus-io.github.io/milvus-helm
helm repo update

# 2. 安装 Milvus Operator
kubectl apply -f https://raw.githubusercontent.com/milvus-io/milvus-operator/main/deploy/manifests/deployment.yaml

# 3. 部署 Milvus Cluster
cat <<EOF | kubectl apply -f -
apiVersion: milvus.io/v1beta1
kind: Milvus
metadata:
  name: milvus
  namespace: milvus
spec:
  mode: cluster
  components:
    proxy:
      replicas: 3
    queryNode:
      replicas: 4
      resources:
        requests:
          cpu: "4"
          memory: "8Gi"
        limits:
          cpu: "8"
          memory: "16Gi"
    dataNode:
      replicas: 2
    indexNode:
      replicas: 2
  config:
    dataCoord:
      enableCompaction: true
    queryNode:
      grpc:
        serverMaxRecvSize: "268435456"  # 256 MB
      segment:
        maxSize: "1024"
EOF

# 4. 检查状态
kubectl get milvus milvus -n milvus
kubectl get pods -n milvus
```

### 3.3 Spring AI 集成 Milvus

```xml
<dependency>
    <groupId>org.springframework.ai</groupId>
    <artifactId>spring-ai-starter-vector-store-milvus</artifactId>
</dependency>
```

```yaml
spring:
  ai:
    vectorstore:
      milvus:
        host: milvus.milvus.svc.cluster.local
        port: 19530
        username: root
        password: Milvus
        database: production
        collection: knowledge_base
        embedding-dimension: 1024
        index-type: HNSW
        metric-type: COSINE
```

```java
@Configuration
public class MilvusConfig {

    @Bean
    public VectorStore milvusVectorStore(EmbeddingModel embeddingModel) {
        MilvusVectorStore.Builder builder = MilvusVectorStore.builder()
            .embeddingModel(embeddingModel)
            .client(MilvusServiceClient.builder()
                .withUri("milvus.milvus.svc.cluster.local:19530")
                .withUsername("root")
                .withPassword("Milvus")
                .build());

        // 自动创建 Collection + 索引
        return builder.build();
    }
}
```

**使用**：

```java
@Service
public class KnowledgeService {

    @Autowired private VectorStore vectorStore;

    /**
     * 摄入文档
     */
    public void ingest(List<Document> chunks) {
        vectorStore.add(chunks);
    }

    /**
     * 检索
     */
    public List<Document> search(String query, int topK) {
        return vectorStore.similaritySearch(
            SearchRequest.builder()
                .query(query)
                .topK(topK)
                .similarityThreshold(0.7)
                .filterExpression("tenant_id == 'acme' && department == 'hr'")  // 标量过滤
                .build()
        );
    }
}
```

### 3.4 Collection Schema 设计

```java
/**
 * 自定义 Collection Schema（精细控制）
 */
@Service
public class KnowledgeCollectionInitializer {

    @Autowired private MilvusClient milvusClient;

    @PostConstruct
    public void init() {
        // 1. 创建 Collection
        CreateCollectionParam param = CreateCollectionParam.newBuilder()
            .withCollectionName("knowledge_base")
            .withDescription("企业知识库")
            .withShardsNum(2)  // 分片数
            .addFieldType(FieldType.newBuilder()
                .withName("id")
                .withDataType(DataType.Int64)
                .withPrimaryKey(true)
                .withAutoID(true)
                .build())
            .addFieldType(FieldType.newBuilder()
                .withName("embedding")
                .withDataType(DataType.FloatVector)
                .withDimension(1024)
                .build())
            .addFieldType(FieldType.newBuilder()
                .withName("content")
                .withDataType(DataType.VarChar)
                .withMaxLength(65535)  // 最大 64KB
                .build())
            .addFieldType(FieldType.newBuilder()
                .withName("tenant_id")
                .withDataType(DataType.VarChar)
                .withMaxLength(64)
                .build())
            .addFieldType(FieldType.newBuilder()
                .withName("department")
                .withDataType(DataType.VarChar)
                .withMaxLength(64)
                .build())
            .addFieldType(FieldType.newBuilder()
                .withName("source")
                .withDataType(DataType.VarChar)
                .withMaxLength(512)
                .build())
            .addFieldType(FieldType.newBuilder()
                .withName("chunk_index")
                .withDataType(DataType.Int32)
                .build())
            .addFieldType(FieldType.newBuilder()
                .withName("created_at")
                .withDataType(DataType.Int64)
                .build())
            .build();
        milvusClient.createCollection(param);

        // 2. 创建索引
        CreateIndexParam indexParam = CreateIndexParam.newBuilder()
            .withCollectionName("knowledge_base")
            .withIndexName("embedding_index")
            .withFieldName("embedding")
            .withIndexType(IndexType.HNSW)
            .withMetricType(MetricType.COSINE)
            .withExtraParam("""
                {
                    "M": 16,
                    "efConstruction": 200
                }
                """)
            .build();
        milvusClient.createIndex(indexParam);

        // 3. 加载到内存（必须！）
        LoadCollectionParam loadParam = LoadCollectionParam.newBuilder()
            .withCollectionName("knowledge_base")
            .withReplicaNumber(2)  // 2 副本（高可用）
            .build();
        milvusClient.loadCollection(loadParam);
    }
}
```

**关键 Schema 设计**：

| 字段 | 类型 | 用途 |
|---|---|---|
| `id` | Int64 (PK, auto) | 主键 |
| `embedding` | FloatVector(1024) | 向量 |
| `content` | VarChar(64K) | 原始文本 |
| `tenant_id` | VarChar(64) | 多租户隔离 |
| `department` | VarChar(64) | 部门隔离 |
| `source` | VarChar(512) | 文档来源（引用溯源）|
| `chunk_index` | Int32 | 分块序号 |
| `created_at` | Int64 | 创建时间 |

### 3.5 HNSW 参数调优

**HNSW 三大参数**：

| 参数 | 含义 | 推荐值 | 影响 |
|---|---|---|---|
| `M` | 每个节点的边数 | 8-32 | 越大越准，但内存越大 |
| `efConstruction` | 建索引时的搜索深度 | 100-400 | 越大索引质量越好，但建索引慢 |
| `ef` | 查询时的搜索深度 | 50-300 | 越大召回率越高，但查询慢 |

**调优公式**：

```
M = 16       （生产默认）
efConstruction = 200  （建索引慢一点但质量高）
ef = max(topK * 2, 100)  （查询时根据 topK 调）
```

**典型场景**：

```java
// 场景 1：低延迟（聊天机器人）
.withExtraParam("""
    {"M": 8, "efConstruction": 100}
""")
// 查询时：ef = 50

// 场景 2：高准确率（企业知识库）
.withExtraParam("""
    {"M": 32, "efConstruction": 400}
""")
// 查询时：ef = 200

// 场景 3：内存受限（边缘部署）
.withExtraParam("""
    {"M": 8, "efConstruction": 100}
""")
// + 量化 INT8
```

### 3.6 混合检索（向量 + BM25）

```java
@Service
public class HybridSearchService {

    @Autowired private MilvusClient milvusClient;
    @Autowired private ElasticsearchClient esClient;

    /**
     * 混合检索：向量 + 关键词，RRF 融合
     */
    public List<Document> hybridSearch(String query, int topK) {
        // 1. 向量检索（Milvus）
        SearchParam vectorSearch = SearchParam.newBuilder()
            .withCollectionName("knowledge_base")
            .withVectorFieldName("embedding")
            .withMetricType(MetricType.COSINE)
            .withTopK(50)
            .addQueryParam("ef", "200")
            .withVectors(List.of(embedQuery(query)))
            .withOutFields(List.of("id", "content", "source"))
            .build();
        SearchResp vectorResp = milvusClient.search(vectorSearch);
        List<ScoredDocument> vectorResults = parseResults(vectorResp);

        // 2. 关键词检索（ES）
        SearchResponse<JsonData> esResp = esClient.search(s -> s
            .index("knowledge_base")
            .size(50)
            .query(q -> q.match(m -> m.field("content").query(query)))
        , JsonData.class);
        List<ScoredDocument> bm25Results = parseEsResults(esResp);

        // 3. RRF 倒数排名融合
        Map<Long, Double> rrfScores = new HashMap<>();
        int k = 60;
        for (int i = 0; i < vectorResults.size(); i++) {
            rrfScores.merge(vectorResults.get(i).id(), 1.0 / (k + i + 1), Double::sum);
        }
        for (int i = 0; i < bm25Results.size(); i++) {
            rrfScores.merge(bm25Results.get(i).id(), 1.0 / (k + i + 1), Double::sum);
        }

        // 4. 排序 + 取 top K
        return rrfScores.entrySet().stream()
            .sorted(Map.Entry.<Long, Double>comparingByValue().reversed())
            .limit(topK)
            .map(e -> getDocument(e.getKey()))
            .collect(Collectors.toList());
    }
}
```

---

## 第四部分：Qdrant 完整实战

### 4.1 Qdrant 优势

- **Rust 写的**（性能强，内存安全）
- **过滤能力极强**（payload-based filtering）
- **单文件存储**（不用 MinIO 之类的对象存储）
- **支持向量量化**（自动 INT8 / Binary）

### 4.2 Qdrant 部署

```bash
# Docker 单机
docker run -d \
  --name qdrant \
  -p 6333:6333 \
  -p 6334:6334 \
  -v /qdrant/storage:/qdrant/storage \
  qdrant/qdrant:latest
```

### 4.3 Spring AI 集成 Qdrant

```xml
<dependency>
    <groupId>org.springframework.ai</groupId>
    <artifactId>spring-ai-starter-vector-store-qdrant</artifactId>
</dependency>
```

```yaml
spring:
  ai:
    vectorstore:
      qdrant:
        host: localhost
        port: 6334
        collection-name: knowledge_base
        embedding-dimension: 1024
```

**Qdrant 的 Payload 过滤**（比 Milvus 更强大）：

```java
// 复杂过滤：tenant AND dept AND date_range
Filter filter = Filter.newBuilder()
    .addMust(Condition.newBuilder()
        .setField("tenant_id")
        .setMatch(Match.newBuilder().setKeyword("acme").build())
        .build())
    .addMust(Condition.newBuilder()
        .setField("department")
        .setMatch(Match.newBuilder().setKeyword("hr").build())
        .build())
    .addMust(Condition.newBuilder()
        .setField("created_at")
        .setRange(Range.newBuilder()
            .setGte(1700000000)
            .setLte(1800000000)
            .build())
        .build())
    .build();
```

---

## 第五部分：Weaviate 完整实战

### 5.1 Weaviate 优势

- **自带向量化和混合检索**
- **GraphQL API**（前端友好）
- **模块化设计**（vectorizer / retriever / generator 都可插拔）
- **强类型 Schema**

### 5.2 Spring AI 集成 Weaviate

```xml
<dependency>
    <groupId>org.springframework.ai</groupId>
    <artifactId>spring-ai-starter-vector-store-weaviate</artifactId>
</dependency>
```

```yaml
spring:
  ai:
    vectorstore:
      weaviate:
        scheme: http
        host: localhost:8080
        object-class: KnowledgeChunk
```

---

## 第六部分：pgvector 完整实战（小团队首选）

### 6.1 pgvector 适用场景

- 已有 PostgreSQL
- 数据量 < 1000 万
- 不想引入新组件
- 强事务需求

### 6.2 启用 pgvector

```sql
-- 1. 安装扩展
CREATE EXTENSION IF NOT EXISTS vector;

-- 2. 创建表
CREATE TABLE knowledge_chunks (
    id BIGSERIAL PRIMARY KEY,
    content TEXT NOT NULL,
    embedding VECTOR(1024),  -- 1024 维
    tenant_id VARCHAR(64),
    department VARCHAR(64),
    source VARCHAR(512),
    created_at TIMESTAMPTZ DEFAULT NOW()
);

-- 3. 创建 HNSW 索引
CREATE INDEX ON knowledge_chunks
USING hnsw (embedding vector_cosine_ops)
WITH (m = 16, ef_construction = 200);

-- 4. 标量字段索引
CREATE INDEX idx_tenant ON knowledge_chunks(tenant_id);
CREATE INDEX idx_dept ON knowledge_chunks(department);
```

### 6.3 Spring AI 集成 pgvector

```xml
<dependency>
    <groupId>org.springframework.ai</groupId>
    <artifactId>spring-ai-starter-vector-store-pgvector</artifactId>
</dependency>
<dependency>
    <groupId>org.postgresql</groupId>
    <artifactId>postgresql</artifactId>
</dependency>
```

```yaml
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/knowledge
    username: postgres
    password: secret
  ai:
    vectorstore:
      pgvector:
        table-name: knowledge_chunks
        dimensions: 1024
        index-type: HNSW
        distance-type: COSINE
```

### 6.4 pgvector 性能调优

```sql
-- 1. 调高维护内存（关键！）
SET maintenance_work_mem = '2GB';

-- 2. 调高并行度
SET max_parallel_maintenance_workers = 4;

-- 3. 查询时调整 ef
SET hnsw.ef_search = 200;  -- 默认 40，调高召回更准

-- 4. 部分索引（如果数据有冷热分层）
CREATE INDEX ON knowledge_chunks
USING hnsw (embedding vector_cosine_ops)
WHERE created_at > NOW() - INTERVAL '90 days';
```

---

## 第七部分：向量数据库高级话题

### 7.1 多租户隔离

**三种模式**：

| 模式 | 描述 | 适合 |
|---|---|---|
| **单 Collection + 过滤** | 所有数据在一个 Collection，filter 过滤 | 100 万 - 1 亿向量 |
| **多 Collection** | 每个租户一个 Collection | 10 万 - 100 万向量 |
| **多 Database** | 每个租户一个 Database（Milvus 2.0+）| 大客户隔离 |

### 7.2 实时增量 + 批量重建

```java
@Service
public class VectorIndexManager {

    /**
     * 实时写入
     */
    public void addChunk(Document chunk) {
        milvusClient.insert(InsertParam.newBuilder()
            .withCollectionName("knowledge_base")
            .addField("content", chunk.getContent())
            .addField("embedding", chunk.getEmbedding())
            .addField("tenant_id", chunk.getMetadata().get("tenant_id"))
            .build());
    }

    /**
     * 批量重建（定期跑）
     */
    @Scheduled(cron = "0 0 3 * * ?")  // 每天凌晨 3 点
    public void rebuildIndex() {
        // 1. 创建临时 Collection
        milvusClient.createCollection("knowledge_base_tmp");
        // 2. 全量重新写入
        reindexAll();
        // 3. 原子切换别名
        milvusClient.alterCollectionAlias(
            "knowledge_base", "knowledge_base_old");
        milvusClient.createCollectionAlias(
            "knowledge_base_tmp", "knowledge_base");
        // 4. 删除旧 Collection
        milvusClient.dropCollection("knowledge_base_old");
    }
}
```

### 7.3 冷热分层

```
热数据（最近 30 天）：HNSW 索引，加载到内存
   ↓
冷数据（30 天前）：IVF 索引，磁盘存储
   ↓
召回：热数据命中 → 直接返回
   冷数据命中 → 异步加载 IVF 索引
```

**实现**（Milvus 支持 Partition）：

```java
// 按月分区
milvusClient.createPartition(CreatePartitionParam.newBuilder()
    .withCollectionName("knowledge_base")
    .withPartitionName("2026_06")
    .build());

// 检索时只查热分区
SearchParam param = SearchParam.newBuilder()
    .withPartitionNames(List.of("2026_06", "2026_05", "2026_04"))
    // ...
    .build();
```

### 7.4 向量备份 + 恢复

```bash
# Milvus 备份（用 milvus-backup 工具）
milvus-backup create \
  --milvus-uri localhost:19530 \
  --backup-name my_backup_20260610

# 恢复
milvus-backup restore \
  --milvus-uri localhost:19530 \
  --backup-name my_backup_20260610
```

### 7.5 监控

```yaml
# Prometheus 抓取 Milvus metrics
scrape_configs:
  - job_name: 'milvus'
    static_configs:
      - targets: ['milvus:9091']
```

**关键指标**：

| 指标 | 含义 | 告警 |
|---|---|---|
| `milvus_query_node_search_latency` | 检索延迟 | P99 > 100ms |
| `milvus_data_node_insert_latency` | 插入延迟 | P99 > 1s |
| `milvus_proxy_req_count` | 请求数 | 异常激增 |
| `milvus_querynode_cache_hit_rate` | 缓存命中率 | < 80% |
| `milvus_segment_count` | 段数 | > 1000 触发合并 |

---

## 第八部分：选型决策清单

**用以下 Checklist 帮你选**：

| 问题 | 选项 | 推荐 |
|---|---|---|
| **数据量 < 100 万？** | 单机方案 | **pgvector** / Qdrant / Weaviate |
| **数据量 > 1000 万？** | 分布式 | **Milvus 集群** |
| **已有 PG 团队？** | 复用 PG | **pgvector** |
| **超大规模 / 亿级？** | 托管 | **Zilliz Cloud** / Pinecone |
| **国内合规（数据不出境）？** | 国产 | **阿里云 ADS** / 腾讯云 VectorDB |
| **性能极致要求？** | Rust 性能 | **Qdrant** |
| **跟 Spring AI 集成最稳？** | 官方支持 | Milvus / Qdrant / Weaviate 都行 |
| **运维最简单？** | 单组件 | **pgvector** |
| **生态最全（备份/监控/工具）？** | 成熟度 | **Milvus** |

---

## 第九部分：生产 Checklist

| 类别 | 检查项 |
|---|---|
| **架构** | Standalone vs Cluster 选型正确 |
| **架构** | 副本数 ≥ 2（高可用）|
| **架构** | Object Storage 配置正确（S3/MinIO）|
| **Schema** | 字段设计含 tenant_id / 时间戳 |
| **Schema** | 标量字段建索引（加速过滤）|
| **索引** | 索引类型选对（HNSW 默认）|
| **索引** | HNSW 参数调优（M=16, ef=200）|
| **索引** | 量化（数据量大时）|
| **检索** | 召回 top 50-100（够 Rerank 用）|
| **检索** | ef_search 根据场景调 |
| **过滤** | 标量 + 向量联合过滤 |
| **混合** | 向量 + BM25 混合检索 |
| **Rerank** | Bi-Encoder + Cross-Encoder 两阶段 |
| **运维** | Prometheus + Grafana 接入 |
| **运维** | 告警规则（延迟/错误率/磁盘）|
| **运维** | 备份策略（每日/每周）|
| **运维** | 灾难恢复演练 |
| **运维** | 慢查询分析 |
| **运维** | 段合并（Milvus compaction）|
| **运维** | 冷热数据分层 |
| **多租户** | 数据隔离（Schema/Collection/Database）|
| **安全** | 认证授权（用户名密码 / RBAC）|
| **安全** | 传输加密（TLS）|
| **安全** | 静态加密（数据落盘）|

---

## 写在最后

**向量数据库不是"装上就行"，是"用对才行"**。

**3 条血泪教训**：

1. **别用 Chroma / LanceDB 上生产**——只适合原型和小项目
2. **pgvector 不适合 > 1000 万向量**——到时候迁移很痛苦
3. **HNSW 索引被刷爆是常见问题**——**一定要加 max_size + 段合并**

**给你 3 条选型建议**：

1. **< 100 万向量 + 已有 PG → pgvector**（最省事）
2. **100 万 - 10 亿向量 + 性能要求 → Milvus 集群**（生产首选）
3. **超大规模 / 不想运维 → Zilliz Cloud / Pinecone**（花钱买时间）

**最后送你 3 句话**：

1. **向量库选错 = 半年后大迁移**——选型比你想的更重要
2. **HNSW 是默认，但要看场景**——海量数据考虑 IVF / ScaNN
3. **混合检索 + Rerank 才是未来**——纯向量检索已经不够用了

**这一篇 + 深挖版 4（RAG 调优），构成完整的"向量检索"知识体系。**

---

